# План действий: 04.06 – 10.06

> Составлен на основании `docs/plans/СВОДНЫЙ_ПЛАН_РЕАЛИЗАЦИИ.md`, итогов встреч 02–03.06 и `docs/specificity.md`.
> Из плана удалены задачи, уже отражённые в документации (`docs/api/*`, `docs/schema/*`, `docs/pipelines/*`, `docs/glossary.md`, `docs/database/db_diagrams.md`).
> Дедлайн готовности сервисов — **понедельник 09.06**; отладка и интеграция — **вторник–среда 10.06**.

---

## Цель недели

Превратить каркасы сервисов в **минимально работающие сервисы** (working API, запуск в Docker, прохождение полного пайплайна на локальном стенде).

---

## 1. Общие задачи (все сервисы)

| #   | Задача                                                                                                                                                                                                                                                                                      | Ответственный                                       | Критерий готовности                                                               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- | --------------------------------------------------------------------------------- |
| 1.1 | **Миграция ID → bigint в коде** — обновить модели, валидаторы, сериализаторы, тесты (нет UUID в API-контрактах). **bbox**: Parser/OCR возвращают raw координаты (px) + размер страницы (`page.width`, `page.height`); Converter-Validator нормирует bbox → [0,1].                           | Каждый разработчик + Игорь Жулин (координация)      | ServiceChecker проходит сценарий с числовыми ID; bbox ∈ [0,1] на выходе Converter |
| 1.2 | **Dockerfile** — каждый сервис в `backend/<service>/` имеет рабочий `Dockerfile`.                                                                                                                                                                                                           | Все разработчики                                    | `docker build` локально успешен                                                   |
| 1.3 | **`.env.example`** — конфиг с флагами `USE_MOCK_*` для переключения заглушка ↔ реальный сервис.                                                                                                                                                                                             | Все разработчики                                    | Переменные задокументированы в `README.md` сервиса                                |
| 1.4 | **ServiceChecker** — расширить до прогона полного сценария: загрузка → парсинг → конвертация → реестр → индексация → поиск.                                                                                                                                                                 | Игорь Жулин<br/>(проверяют у себя все разработчики) | Автоматический прогон без ручного вмешательства                                   |
| 1.5 | **Синхронизация документации с кодом** — привести `docs/api`, `docs/schema`, `docs/database`, `docs/glossary` в соответствие с решениями 04.06 (bigint, bbox); закрыть оставшиеся TODO аудита (фильтры `sort_by`/`order`, `top_k` ограничение, уточнение `source_type` vs `document_type`). | Игорь Жулин                                         | Нет расхождений между кодом и docs                                                |

---

## 2. Задачи по сервисам

| Сервис                  | Задача                                                                                                                                                                                                                                                                      | Ответственный              | Критерий готовности                                                    |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------- |
| **Parser**              | Отладить pipeline парсинга текстового слоя. Working API: принимает PDF (DOCX?), возвращает `raw_ocr_v4` с **raw bbox (px) + размер страницы** (`page.width`, `page.height`). Нормирование bbox → [0,1] — ответственность Converter-Validator.                               | Диана                      | Успешный прогон тестового PDF (DOCX?)                                  |
| **OCR**                 | Доработать pipeline OCR для сканов (Lama Parser). Working API: принимает изображение/PDF без текстового слоя, возвращает `raw_ocr_v4` с **raw bbox (px) + размер страницы** (`page.width`, `page.height`). Нормирование bbox → [0,1] — ответственность Converter-Validator. | Дмитрий                    | Успешный прогон тестового скана                                        |
| **Converter-Validator** | Реализовать базовый функционал: иерархия, метаданные, валидация, **нормирование bbox (raw px → 0..1) по размеру страницы из raw_ocr_v4**. Принимает `raw_ocr_v4`, отдаёт `validated_v3`. Поддержка **bigint** ID; валидация **bbox ∈ [0,1]**.                               | Игорь Колесов              | Успешная конвертация JSON от Parser и OCR                              |
| **Registry**            | Схемы БД: `bigint` PK/FK, sequences (`documents`, `document_versions`, `document_references`, `document_history`, `sessions`, `messages`, `tasks`). Реализовать `check-uniqueness`, сохранение карточки документа, `references`.                                            | Андрис                     | Документ сохраняется в БД, проверка уникальности работает              |
| **RagBuilder**          | Полный цикл индексации: получение данных из Registry → чанкование → эмбеддинги → сохранение в `pgvector`. Поддержка **bigint `document_id`**.                                                                                                                               | Игорь / Павел              | Успешная индексация тестового документа                                |
| **RagSearch**           | Гибридный поиск (векторный + BM25) + цитирование источников. Поддержка **bigint `document_id`**; ограничение `top_k ∈ [1, 100]`.                                                                                                                                            | Виталий / Павел            | Поиск возвращает релевантные чанки с ссылками на документы             |
| **QueryService**        | Микропайплайн чата: история → обогащение запроса → `RagSearch` → LLM → цитирование.  **Добавить `project_id`** в сессии чата (создание, редактирование, фильтрация, история).                                                                                               | Роман / Елена              | Ответ на вопрос со ссылками на источники; чаты группируются по проекту |
| **Orchestrator**        | Управление FSM Pipeline 1 (preview → full), longpoll.                                                                                                                                                                                                                       | Архитектор / ответственный | Корректная маршрутизация задач между сервисами                         |
| **UI**                  | Числовые ID в роутах и запросах; согласование фильтров истории, справочников, прав/ролей с бэкендом.                                                                                                                                                                        | Сергей                     | Корректная работа с bigint ID, готовность к тестовому деплою           |
| **Gateway**             | Теперь выступает как реальный прокси для вэба + режим эмуляции. Проксирование с учётом bigint ID; rate limiting уже задокументирован.                                                                                                                                       | Роман                      | Пропускает трафик без ошибок маршрутизации                             |

---

## 3. Задачи групп (операции вне сервисной логики)

| Группа                            | Задача                                                                                                                                                                                | Ответственный                        | Примечание                                                    |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------- |
| **Группа 1 (Парсинг / OCR)**      | Унификация `raw_ocr_v4` — единый контракт для Parser и OCR; совместная отладка стыка с Converter-Validator.                                                                           | Диана + Дмитрий                      | Убедиться, что оба сервиса выдают идентичную структуру        |
| **Группа 2 (БД / Поиск / RAG)**   | ; подготовка методологии экспериментов по индексации (параметры: чанки, перекрытие, модель эмбеддинга, реранкинг).                                                                    | Павел + Семён + Игорь Жулин + Полина | Эксперименты — приоритет Спринта 2, методология нужна к 10.06 |
| **Группа 3 (UI / UX)**            | Согласование финальной модели **RBAC**, фильтров истории (дата, тема, название чата, **проект**), карточек разделов с бэкендом и Семёном; подготовка к тестовому деплою UI + Gateway. | Сергей + Игорь Жулин + Семён         | Без массовых встреч — в рамках четверг–пятница                |
| **Группа 4 (бэк)**                | Согласование структуры БД и sequences; **добавление таблицы `chat.projects`** и поля `project_id` в **`chat.sessions`**<br/>Gateway для вэб                                           | Роман + Роман                        |                                                               |
| **Группа 5 (Качество / Метрики)** | Обновление 35 эталонных вопросов с привязкой к **числовым `document_id`**; получение актуальных редакций документов от Семёна.                                                        | Полина + Семён                       | База для экспериментов Спринта 2                              |
| **Инфраструктура**                | `docker-compose.yml` (RagBuilder + Postgres + MinIO); настройка авто-деплоя; решение вопроса GPU.                                                                                     | Игорь (админ)                        | Локальный стенд работает по `docker compose up`               |
| **Архитектура**                   | Актуализация Excel-таблицы статусов сервисов; формулировка пошаговой задачи для экспериментов по индексации/поиску (для всех желающих, включая Полину).                               | Игорь Жулин                          | Документ с методологией тестирования                          |

---

## 4. Открытые вопросы, требующие решения на неделе

| #   | Вопрос                                                        | Приоритет      | Кто решает                   | Крайний срок         |
| --- | ------------------------------------------------------------- | -------------- | ---------------------------- | -------------------- |
| 4.1 | `comparison_id`, `batch_id` — назначение и формат             | 🔴 Блокирующий | Архитектор                   | 10.06                |
| 4.2 | Схема `approve` / `reprocess` документа (end-to-end FSM)      | 🔴 Блокирующий | Архитектор + Семён + Павел   | 10.06 (решение)      |
| 4.3 | `raw_ocr_v2` vs `raw_ocr_v4` — окончательная версия контракта | 🟡 Средний     | Архитектор + Диана + Дмитрий | 10.06                |
| 4.4 | RBAC: финальная модель прав и вкладок админки                 | 🟡 Средний     | Сергей + Игорь Жулин + Семён | 10.06 (согласование) |

---

## 5. Чек-лист приёмки (10.06)

- [ ] Все сервисы собираются в Docker локально.
- [ ] ServiceChecker проходит полный сценарий с **bigint** ID.
- [ ] Parser/OCR возвращают raw bbox (px) + размер страницы; Converter-Validator нормирует bbox в **[0, 1]**.
- [ ] RagBuilder успешно индексирует тестовый документ в `pgvector`.
- [ ] RagSearch возвращает релевантные чанки с цитированием.
- [ ] QueryService отвечает на вопрос со ссылками на источники (LLM + RAG).
- [ ] UI корректно работает с числовыми ID; фильтры и справочники согласованы.
- [ ] Документация синхронизирована с кодом (нет расхождений типов ID и bbox).
- [ ] Определены `comparison_id` / `batch_id` или принято решение об их удалении.
- [ ] Схема `approve`/`reprocess` описана и согласована.

---

## 6. График встреч

---

> ⚠️ **Пункт 1.4 (CI/CD) удалён** — доступ к Git не предоставлен. CI/CD отложен.

| День                         | Формат                                      | Участники        |
| ---------------------------- | ------------------------------------------- | ---------------- |
| **Среда 04.06**              | Общая встреча (статус, анонс bigint/bbox)   | Все              |
| **Четверг–Пятница 05–06.06** | Встречи подгрупп (гр. 1, гр. 2, UI)         | По направлениям  |
| **Понедельник 09.06**        | Дедлайн сдачи сервисов в `develop`          | Все разработчики |
| **Вторник–Среда 10.06**      | Отладка, интеграция, подготовка к демо      | Все              |
| **Среда 10.06, 18:00**       | Общая встреча: демо, статус, план Спринта 2 | Все              |

---
