# План действий: 11.06 – 17.06

> Составлен на основании `docs/plans/СВОДНЫЙ_ПЛАН_РЕАЛИЗАЦИИ.md`, итогов Спринта 1 и `docs/specificity.md`.
> Дедлайн готовности — **понедельник 16.06**; отладка и тесты качества — **вторник–среда 17.06**.

---

## Цель недели

Провести **полный интеграционный прогон** и **тест качества** системы: от загрузки документа до ответа чата с измерением метрик.

---

## 1. Общие задачи (все сервисы)

| #   | Задача | Ответственный | Критерий готовности |
| --- | ------ | ------------- | ------------------- |
| 1.1 | **Интеграционный прогон** — прогнать полный сценарий end-to-end: загрузка → парсинг → конвертация → реестр → индексация → поиск → ответ чата. | Все разработчики | ServiceChecker проходит сценарий без ручного вмешательства; нет ошибок `500`/`501` |
| 1.2 | **Отладка стыков** — устранить расхождения в API-контрактах (поля, типы, bbox, bigint) между сервисами. | Игорь Жулин + все | Нет ошибок валидации схем при обмене между сервисами |
| 1.3 | **UI на реальном бэкенде** — переключить UI с моков на реальные сервисы; проверить экраны «Загрузка документа» и «База знаний». | Сергей | UI работает с реальными endpoint'ами |

---

## 2. Тест качества (КРИТИЧЕСКИЙ ПРИОРИТЕТ)

| #   | Задача | Ответственный | Критерий готовности |
| --- | ------ | ------------- | ------------------- |
| 2.1 | **Тестовая база знаний** — сформировать эталонный набор документов (минимум 50 шт.) и 35 эталонных вопросов с ожидаемыми ответами. | Полина + Семён | Набор документов и вопросов загружен в систему |
| 2.2 | **Индексация тестовой базы** — прогнать все документы через Pipeline 1 и Pipeline 2; убедиться, что все проиндексированы в `pgvector`. | Павел | Все тестовые документы доступны в поиске |
| 2.3 | **Измерение precision / recall** — на 35 эталонных вопросах измерить точность и полноту поиска (гибридный векторный + BM25). | Полина + Павел | Зафиксированы метрики precision@k, recall@k, MRR |
| 2.4 | **Оценка качества ответов чата** — проверить, что QueryService возвращает осмысленные ответы с корректным цитированием источников (документ, страница, bbox). | Елена + Роман | ≥ 70% ответов содержат релевантные цитаты из правильных документов |
| 2.5 | **Стабильность и ошибки** — зафиксировать процент отказов (fallback / 500 / пустой ответ) на тестовой выборке. | Игорь Жулин | ≤ 10% отказов на полном сценарии |
| 2.6 | **Отчёт по качеству** — сводный документ: метрики, найденные проблемы, рекомендации по улучшению (модели эмбеддингов, чанкинг, реранкинг). | Полина + Игорь Жулин | Отчёт в `docs/plans/quality_report_sprint2.md` или аналогичном месте |

---

## 3. Задачи по сервисам (доработки после Спринта 1)

| Сервис | Задача | Ответственный | Критерий готовности |
| ------ | ------ | ------------- | ------------------- |
| **Converter-Validator** | Устранить ошибки нормирования bbox; обеспечить валидацию структуры `validated_v3` на реальных выходах Parser/OCR. | Игорь Колесов | Успешная конвертация ≥ 90% тестовых документов |
| **RagBuilder** | Устранить ошибки чанкования и сохранения в `pgvector` на больших документах. | Павел | Индексация проходит без падений на документах > 100 страниц |
| **RagSearch** | Настроить гибридный вес вектор/BM25; убедиться, что `top_k` ограничение [1, 100] работает. | Виталий / Павел | Поиск возвращает релевантные чанки в пределах `top_k` |
| **QueryService** | Устранить ошибки микропайплайна чата; стабилизировать подключение к LLM и RagSearch. | Роман / Елена | Чат отвечает без таймаутов на 35 эталонных вопросах |
| **Registry** | Устранить ошибки `check-uniqueness` и сохранения `references` на больших объёмах. | Андрис | Уникальность и ссылки корректны на тестовой базе |
| **Orchestrator** | Стабилизировать FSM Pipeline 1; устранить race condition при дублированных задачах. | Архитектор / ответственный | FSM проходит preview → full без зависаний |
| **Gateway** | Устранить ошибки проксирования при больших файлах (> 50 МБ); rate limiting без ложных срабатываний. | Роман | Пропускает файлы до 100 МБ, не режет легитимный трафик |
| **UI** | Исправить ошибки работы с числовыми ID, фильтры истории, отображение справочников. | Сергей | Нет UI-ошибок, блокирующих тест качества |

---

## 4. Открытые вопросы, требующие решения на неделе

| #   | Вопрос | Приоритет | Кто решает | Крайний срок |
| --- | ------ | --------- | ---------- | ------------ |
| 4.1 | **Минимальные пороговые значения precision / recall** — какие метрики считаем приемлемыми для MVP? | 🔴 Высокий | Полина + Архитектор | 17.06 |
| 4.2 | **Решение по GPU** — успеваем ли получить/настроить GPU для эмбеддингов и LLM до Спринта 3? | 🟡 Средний | Игорь (админ) + Архитектор | 17.06 |
| 4.3 | **Модель эмбеддингов и чанкинг** — выбрать финальные параметры на основе теста качества. | 🟡 Средний | Павел + Полина | 17.06 |
| 4.4 | **Пользовательские категории документов** — окончательное решение по архитектуре `registry.categories` и UI. | 🟡 Средний | Архитектор + Сергей | 17.06 |

---

## 5. Чек-лист приёмки (17.06)

- [ ] Полный интеграционный прогон проходит без ручного вмешательства.
- [ ] UI подключён к реальному бэкенду.
- [ ] Тестовая база из ≥ 50 документов загружена и проиндексирована.
- [ ] Precision / recall измерены на 35 эталонных вопросах; метрики зафиксированы.
- [ ] QueryService цитирует источники корректно в ≥ 70% случаев.
- [ ] Отчёт по качеству составлен и выложен в `docs/plans/`.
- [ ] Определены пороговые метрики качества для MVP.
- [ ] Выбраны финальные параметры индексации (модель, чанкинг, гибридный вес).

---

## 6. График встреч

| День | Формат | Участники |
| ---- | ------ | --------- |
| **Понедельник 16.06** | Дедлайн сдачи доработок после Спринта 1 | Все разработчики |
| **Вторник–Среда 17.06** | Тест качества, интеграционный прогон, отладка | Все |
| **Среда 17.06, 18:00** | Общая встреча: отчёт по качеству, метрики, план Спринта 3 | Все |

---

> ⚠️ **Спринт 2 фокусируется на качестве и стабильности.** Новый функционал (пользовательские категории, RBAC, админка) — приоритет Спринта 3, если ресурсы позволяют.
