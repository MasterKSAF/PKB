# TODO по итогам аудита документации (29.05.26)

## Что сделано (закоммичено)

### Фаза 0 — Блокирующие
- [x] 0.1 — Несуществующие эндпоинты удалены из common_api.md
- [x] 0.2 — Формат ответа уточнён
- [x] 0.3 — Статусы синхронизированы (draft удалён, pending → pending_index, idle — виртуальный)
- [x] 0.4 — source_type синхронизирован (db_diagrams.md)
- [x] 0.5 — Типы секций исправлены в CHECK
- [x] 0.6 — Действия decide унифицированы (3 действия)
- [x] 0.7 — FSM: недостающие переходы, таймауты, reprocess

### Фаза 1 — Серьёзные (API)
- [x] 1.1 — Маппинг task_id → document_id, таблица идентификаторов
- [x] 1.2 — bbox: система координат по этапам
- [x] 1.3 — Пример validation_errors
- [x] 1.4 — Коды ошибок расширены (сервис-специфичные)
- [x] 1.5 — RBAC-матрица дополнена
- [x] 1.6 — Longpoll min/max (0..60)
- [x] 1.7 — Статусы синхронизированы

### Фаза 2 — Важные
- [x] 2.1 — Per-stage таймауты Pipeline 3
- [x] 2.3 — Обработка ошибки LLM (LLM_GENERATION_FAILED)
- [x] 2.4 — Маппинг section → chunk в глоссарии
- [x] 2.6 — DELETE-поведение (soft/hard)
- [x] 2.7 — Отличия OCR vs Parser
- [x] 2.8 — Таблица идентификаторов
- [x] 2.9 — Версионирование API

### Фаза 3 — Косметика
- [x] 3.3 — Локализация ошибок (message/code)
- [x] 3.5 — Ссылки на common_api.md в сервисных API

---

## Что осталось (TODO)

### Фаза 2 — Важные (не сделано)

#### 2.5 — Параметры фильтрации и сортировки для list-эндпоинтов
**Файлы:** `orchestrator_service_api.md`, `query_service_api.md`, `registry_service_api.md`, `auth_service_api.md`  
**Что нужно:** Добавить параметры `sort_by`, `order`, фильтры по статусу/типу/дате для:
- `GET /documents`
- `GET /documents/queue`
- `GET /chat/sessions`
- `GET /chat/history`
- `GET /classifiers`
- `GET /terminology`
- `GET /admin/users`
- `GET /admin/audit`

---

#### 2.10 — Уточнение Rate Limiting
**Файл:** `common_api.md`  
**Что нужно:** Проверить, что все конкретные эндпоинты чата и поиска перечислены в таблице Rate Limiting (частично сделано через 0.1, но нужна полная верификация).

---

### Фаза 3 — Косметика (не сделано)

#### 3.1 — Двусмысленные формулировки
**Файлы:** Все документы  
**Что нужно:** Поиск и замена фраз:
- «при необходимости» → конкретное условие
- «пользователь может» → «пользователь обязан / имеет право»
- «система должна быстро ответить» → измеримые критерии
- «иногда» → перечисление случаев
- «по умолчанию» → указать возможность изменения

---

#### 3.2 — Единые заголовки секций
**Файлы:** `orchestrator_service_api.md`, `query_service_api.md`, `pipeline1-formation.md`, `overview.md`  
**Что нужно:** Унифицировать названия:
- «Пайплайн 1: Формирование документа» (не «Pipeline 1», не «Пайплайн 1 (Формирование)»)
- «preview-фаза» / «фаза превью» — выбрать один вариант
- Статусы: везде `awaiting_decision` (не `waiting_for_decision`)

---

#### 3.4 — Ограничение top_k для RAG Search
**Файл:** `rag_search_service_api.md`  
**Что нужно:** Добавить `top_k ∈ [1, 100]`, при превышении — `400 BAD_REQUEST`

---

#### 3.6 — source_type vs document_type
**Файлы:** `orchestrator_service_api.md`, `analyse_service_api.md`, `common_api.md`  
**Что нужно:** Уточнить различие:
- `source_type` — тип нормативного документа (GOST, OST, TU...)
- `document_type` — тип проектного документа (normative, drawing, specification...)
- При необходимости — добавить пояснение в common_api.md

---

### Дополнительные замечания из кросс-проверки

- `chat.sessions.document_ids UUID[]` — нарушение 1НФ (но требует изменения схемы БД)
- `messages.sources jsonb` — невозможны запросы по document_id (но требует изменения схемы)
- Нет soft-delete (`deleted_at`) в `documents` (но требует изменения схемы)
- Рекомендация создать OpenAPI 3.x спецификацию

---

## ✳️ Новая партия: отложенные правки после второго раунда аудита (коммит 8055b7d)

### ❌ Неисправленные замечания

| № | Проблема | Где править | Оценка |
|---|----------|-------------|--------|
| 1 | `POST /registry/documents` — нет полной JSON-схемы тела запроса (сервис получает иерархический JSON от Converter-validator, но структура в документации не описана) | `registry_service_api.md`, секция 3.3 | 1 ч |
| 2 | `POST /documents/{doc_id}/approve` — не указана обязательность полей `force` и `comment`, нет описания поведения при `force=true` | `orchestrator_service_api.md` | 0.5 ч |
| 3 | `POST /registry/documents/check-uniqueness` — нет примера тела запроса (какие метаданные передавать для проверки) | `registry_service_api.md`, секция 3.2.5 | 0.5 ч |
| 4 | `task_id` vs `document_id` — на каком этапе какой ID используется. Фраза «пайплайн работает по task_id, document_id появляется позже» требует явной фиксации в тексте пайплайна | `pipeline1-formation.md`, этапы Preview и Full | 1 ч |
| 5 | Судьба JSON-контейнера при `review_required` — при уходе в `archived` по таймауту 48ч что происходит с данными? Надо описать: очистка MinIO, хранение JSON в истории | `pipeline1-formation.md`, секция «Защита от зависших состояний» | 0.5 ч |

### 🔧 Косметические и архитектурные (отложено)

| № | Проблема | Тип | Требует согласования |
|---|----------|-----|---------------------|
| 6 | `registry.documents.group` → переименовать в `project_group` | Консистентность | С командой разработки |
| 7 | Составной индекс `(processing_status, updated_at)` для Scheduler | Производительность | С DBA |
| 8 | Индекс на `chat.messages.session_id` | Производительность | С DBA |
| 9 | Партиционирование `rag.document_chunks` при >10⁷ записей | Масштабирование | С DBA |
| 10 | `processing_status` vs `status` — унифицировать именование поля | Консистентность | С командой |
| 11 | IDOR: engineer может видеть чужие документы (row-level security) | Безопасность | С архитектором |
| 12 | Двусмысленные формулировки в UI-UX документах («система должна уметь уточнять контекст») | Качество | С UI/UX командой |

### ✅ Что сделано в коммите 8055b7d

- Удалён поиск из Orchestrator
- Очищена RBAC-матрица (удалены недокументированные эндпоинты)
- Удалён `chunk_container_id`
- `content_hash_sha256` → `file_hash_sha256` везде
- Таймауты `parsing` (15 мин) и `validation` (30 мин)
- `review_required` → `archived` при таймауте 48ч
- Описаны 4 эндпоинта страниц
- Создан глоссарий (`docs/glossary.md`)
- Исправлены двусмысленные формулировки в `common_api.md` и `converter_validator_service_api.md`
- OpenAPI — решено не делать (Markdown достаточно)

---

## Статистика

| Метрика | Значение |
|---------|----------|
| Всего задач по плану | 30 |
| Выполнено | 19 + 10 |
| Осталось (TODO) | 6 (старые) + 5 (новые) |
| Не вошло в объём (схема БД) | 5 |
| Отредактировано файлов | 18 + 10 |
| Добавлено строк | 232 + 265 |
| Удалено строк | 132 + 102 |
