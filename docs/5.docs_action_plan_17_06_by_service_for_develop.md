# Задания по сервисам: доработка документации

> **Дата**: 17.06.2026
> **Источник**: сверка `docs_plans/` ↔ `docs/` — план `5.docs_action_plan_17_06.md`
> **Формат**: задачи сгруппированы по сервисам/компонентам. Каждая задача — конкретное изменение в конкретном файле.
>
> **Дополнение от 17.06**: добавлены задачи в существующие сервисные разделы — **Parser** (PS-3, PS-4 — P12-1, P12-2, P12-3), **OCR** (OC-4..OC-7 — P12-1, P12-2, P12-3, P12-6), **Orchestrator** (OR-5..OR-7 — P12-1, P12-3, P12-4), **RAG Search** (RS-6..RS-9 — P13-1..3, P12-5, P12-6), **RAG Builder** (RB-4, RB-5 — P13-1), **Registry** (RG-6..RG-9 — P12-4, P12-5, P13-2), **Common API** (CM-5 — P11-1..3), **Gateway** (GW-9 — P11-2), **Overview** (OV-4, OV-5 — P11-5..8). Новые файлы: NEW-14..NEW-17 (спецификации + тесты). Новый раздел **22. Тесты качества** (T-1..T-13).
>
> **Легенда**: 🔴 блокирующе / 🟠 серьёзно / 🟡 важно / 🔵 средне / ⚪ косметика

---

## 1. Gateway Service (`docs/api/gateway_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| GW-1 | Добавить раздел «Аутентификация service-to-service» | 🔴 P0-5 | Описать mTLS/X-Service-Token для внутренних вызовов. Сейчас Gateway проксирует запросы без взаимной аутентификации сервисов |
| GW-2 | Описать X-Internal-Token для internal-эндпоинтов | 🟡 P0-6 | Gateway должен проверять X-Internal-Token при проксировании к internal-эндпоинтам Registry |
| GW-3 | Разделить demo/prod режимы | 🟠 P1-5 | В prod-режиме ошибки API не должны маскироваться mock-данными. Добавить секцию «Режимы работы: demo/production» |
| GW-4 | Rate limiting: реализовать Nginx + Redis | 🟠 P3-1 | Документация честная: `common_api.md:389-392` с пометкой «⏳ требует реализации», таблица лимитов есть (строки 395-407). **Проблема в коде**, не в документации |
| GW-5 | mTLS Gateway ↔ Auth | 🟠 P3-2 | Описать сетевую изоляцию для `/internal/auth/validate` |
| GW-6 | IDOR: примечание о rate-limit и audit | 🟠 P3-3 | Добавить предупреждение о защите от перебора `draft_id`, `document_id`, `session_id` |
| GW-7 | Запрет PII в query-string | 🟠 P3-4 | Добавить явный запрет на передачу токенов/PII в query-параметрах |
| GW-8 | Описать структуру mock-роутера | 🔵 P9-3 | Раскрыть архитектуру mock-режима: как объединены Gateway, Orchestrator и сервисы в единое FastAPI-приложение на порту 8081 |
| GW-9 | Генерировать `X-Request-ID` (если отсутствует) и пробрасывать в логи + downstream | 🟠 P11-2 | На входе в Gateway: если заголовка нет — генерировать UUIDv4. Пробрасывать во все downstream-вызовы. Сохранять в логах (CM-5) |
| GW-10 | Инициализировать OTEL SDK в `main.py` + описать интеграцию с `service_checker` | 🟠 P11-5, P11-9 | (1) `OTLPSpanExporter(endpoint='signoz-otel-collector:4317')`; (2) обязательные атрибуты ресурса (`service.name='gateway'`, `service.version`, `deployment.environment=dev/stage/prod`); (3) `service_checker gateway --post-deploy` — успешный exit-code 0 при наличии OTEL и валидном экспорте |

---

## 2. Registry Service (`docs/api/registry_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| RG-1 | Описать защиту PATCH /registry/documents/{id}/status | 🟡 P0-6 | Internal-эндпоинт, доступен только через Gateway. Описать X-Internal-Token |
| RG-2 | Описать current_version_id | 🟠 P1-21 | Поле есть в БД, API должно возвращать и принимать |
| RG-3 | API аудит (закрыть B7a) | 🟠 P1-1 | Добавить недостающие коды ошибок, примеры, форматы |
| RG-4 | Пометить эндпоинты, требуемые UI | 🟠 P1-8 | `/classifiers`, `/terminology`, `/stats`, `/enums` — CRUD помечены как используемые UI |
| RG-5 | Разделить поля PATCH /registry/documents/{id} на editable/immutable/read-only | 🟡 D14 | Сейчас не разделено. Нужно 3 группы. Решение UI-команды 16.06 |
| RG-6 | Добавить поля `valid_from` (date, NOT NULL) и `valid_until` (date, **NOT NULL**) в `registry_documents` | 🟠 P12-5 | (1) `POST /registry/documents` — оба поля **обязательные**; (2) `PATCH /registry/documents/{id}` — оба editable (группа editable, см. D14); (3) добавить CHECK `valid_until >= valid_from`; (4) добавить INDEX `(valid_from, valid_until)`. **Уточнение 17.06**: `valid_until` **не может быть NULL** — бессрочные документы получают `valid_until = dateMax`. **Для PostgreSQL**: `dateMax = 'infinity'::date` (нативное значение PG, корректно работает в B-tree и сравнениях). **Альтернатива** для совместимости с не-PG-клиентами: `dateMax = '9999-12-31'`. Выбор конвенции — в `validity_dates_spec.md` (NEW-16), глобально для всего проекта |
| RG-7 | Добавить фильтр `?valid_at=YYYY-MM-DD` в `GET /registry/documents` | 🟠 P12-5 | Фильтрация: `valid_from <= valid_at AND valid_until >= valid_at`. Если параметр не указан — без фильтра (текущее поведение). Благодаря `dateMax = 'infinity'::date` фильтрация **автоматически** корректно работает для бессрочных документов — никакой специальной обработки в запросе не требуется |
| RG-8 | Создать `GET /registry/search?q=...` (BM25-only по заголовкам) | 🟠 P13-2 | Поиск по `registry_documents.title` + `doc_code` + `classifier_links` через `pg_trgm` + `tsvector` + `ts_rank`. **Не** использовать Vector-поиск (RAG Search отвечает за содержимое). `top_k` default 20, max 100 |
| RG-9 | Описать связь черновик → `document_id` / `version_id` в API Registry | 🟠 P12-4 | `POST /registry/documents` (`:1050, 1097`) — добавить опциональное `source_draft_id: bigint`. После создания документа возвращать `version_id` в ответе (для записи в `registry_drafts.version_id`) |
| RG-10 | Добавить поле `preview_snapshot` (JSONB, nullable) в ответ GET /registry/documents/{id} | 🟡 | Поле содержит исходный JSON ответа Converter-validator preview, копируется из `registry.drafts.preview_metadata` при approve документа. Добавить в пример ответа и список ключевых полей. Для истории и аудита, не участвует в поиске |

---

## 3. Query Service (`docs/api/query_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| QS-1 | Исправить примеры document_id на bigint | 🔴 P0-2 | Строки 253, 262: заменить `doc-norm-001` → `42`, `sec-4.2` → `420042` |
| QS-2 | Описать longpoll на сообщение чата | 🟠 P1-4 | `GET /chat/sessions/{id}/messages/{msg_id}?longpoll=15` |
| QS-3 | POST /chat/sessions: document_ids, options, project_id | 🟠 P1-6 | Описать поля `document_ids`, `options.model`, `options.temperature`, `project_id` |
| QS-4 | GET /chat/history/export и POST /chat/sessions/{id}/export (stream) | 🟠 P1-7 | Контракт изменён — файл возвращается потоком, без промежуточного URL |
| QS-5 | API аудит (закрыть B7a) | 🟠 P1-1 | Проверить полноту документации |

---

## 4. Orchestrator Service (`docs/api/orchestrator_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| OR-1 | Добавить в админку раздел «Артефакты и журналы обработки» | 🟠 P1-9 | `/tasks/*` — read-only для admin. Описать как UI-раздел |
| OR-2 | Описать повторный запуск POST /drafts/{draft_id}/preview (409) | 🟠 P1-19 | Идемпотентность: если preview уже запущен — 409 |
| OR-3 | Добавить `metadata_overrides` в PATCH /drafts/{id}/decide | 🟡 D13 | Поле со снимком: doc_code, title, mks_oks_code, categories. Решение UI-команды 16.06 |
| OR-4 | Заменить `uploaded_at/uploaded_by` на `created_at/created_by` в ответе versions | ⚪ D21 | Строки 265-275. P2-10 унификация нейминга |
| OR-5 | Передавать `draft_id` в запросах к Parser/OCR | 🔴 P12-1 | В `POST /drafts/{id}/preview` (`:1163-1192`) и при синхронной обработке формировать запрос к Parser/OCR с обязательным `draft_id`. Дополнить таблицу параметров `POST /parser/process` / `POST /ocr/process` в API-документации Оркестратора |
| OR-6 | Записывать `issues[]` от Parser/OCR в таблицу `pipeline.draft_issues` | 🟠 P12-3 | После получения результата от Parser/OCR: `INSERT INTO pipeline.draft_issues (draft_id, source, code, severity, message, location, created_at)` для каждой записи. Добавить endpoint `GET /drafts/{id}/issues` (read-only) |
| OR-7 | Связать черновик с документом и версией: добавить в `GET /drafts/{id}` поля `document_id`, `version_id`, `is_new_document` | 🟠 P12-4 | Сейчас `version_id` присутствует только в `POST /registry/documents`. Поля у черновика **нет**. Зафиксировать контракт: `is_new_document=true` → создаётся новый `document_id` + `version_id=1`; `is_new_document=false` → привязка к существующему `document_id` + новая версия |
| OR-8 | Инициализировать OTEL SDK в `main.py` + описать интеграцию с `service_checker` | 🟠 P11-5, P11-9 | (1) `OTLPSpanExporter(endpoint='signoz-otel-collector:4317')`; (2) `service.name='orchestrator'`, версия, окружение; (3) `service_checker orchestrator --post-deploy` |
| OR-9 | Обновить примеры `preview_metadata` в ответах черновиков (5 примеров в 4 эндпоинтах) | 🟡 | Поля привести к табличным именам: `mks_oks_code`, `okstu_code`, `udk_code`, `pkb_codes[]`. Добавить `era`, `validity_status`, `issuing_body`, `jurisdiction`, `source_type`, `language`, `title_hash_sha256`. Затронутые эндпоинты: GET /drafts (2 примера), GET /drafts/{id}, GET /drafts/{id}/preview, GET /drafts/{id}/preview/status. Сверить с актуальным ответом Converter-validator preview |

---

## 5. Auth Service (`docs/api/auth_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| AU-1 | API аудит (закрыть B7a) | 🟠 P1-1 | Добавить недостающие коды ошибок, примеры |
| AU-2 | Описать ROLES как отдельную таблицу | 🟠 P1-12 | `GET /admin/roles` возвращает список ролей. Описать модель |

---

## 6. Parser Service (`docs/api/parser_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| PS-1 | Добавить аутентификацию service-to-service | 🔴 P0-5 | mTLS/X-Service-Token |
| PS-2 | Добавить поле warnings[] в ответ | 🟠 P3-5 | WARN-лог для PDF-security, обработка не блокируется |
| PS-3 | Принимать `draft_id` в `POST /parser/process` (обязательный параметр запроса) | 🔴 P12-1 | Сейчас `draft_id` не передаётся (`:45-72`). Parser должен возвращать `quality` и `issues` в привязке к `draft_id`. Также передавать в логах (`X-Draft-ID`) |
| PS-4 | Добавить в ответ блок `quality` два флага + массив `issues[]` | 🔴 P12-2, 🟠 P12-3 | (1) `quality.reprocess_required: bool` — порог `avg_confidence < threshold_reprocess` (default `0.6`, настраивается); (2) `quality.operator_confirmation_required: bool` — `threshold_reprocess ≤ avg_confidence < threshold_operator` (default `0.85`) ИЛИ `pages_failed > 0` ИЛИ `lama_fallback_used == true`. (3) `issues[]: [{code, severity, message, location: {page, block}, suggested_action}]`. Источник: гр1 16.06, P12-2, P12-3 |

---

## 7. OCR Service (`docs/api/ocr_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| OC-1 | Добавить аутентификацию service-to-service | 🔴 P0-5 | mTLS/X-Service-Token |
| OC-2 | Добавить поле warnings[] в ответ | 🟠 P3-5 | WARN-лог для PDF-security |
| OC-3 | Зафиксировать риск Lama Parser (облако) | 🟡 P3-6 | Для конфиденциальных документов — альтернативы |
| OC-4 | Принимать `draft_id` в `POST /ocr/process` (обязательный параметр запроса) | 🔴 P12-1 | Аналогично PS-3. Сейчас `:45-70` `draft_id` не описан |
| OC-5 | Добавить в ответ блок `quality` два флага + массив `issues[]` | 🔴 P12-2, 🟠 P12-3 | Идентично PS-4. OCR должен помечать `operator_confirmation_required=true` при `avg_confidence` ниже порога или `pages_failed > 0` (особенно для рукописных/сканированных текстов) |
| OC-6 | Принимать `version_id` (опционально) в `POST /ocr/process` для совместимости с `parsing_specifications.md:5` | ⚪ | ⚠️ Противоречие: `parsing_specifications.md:5` упоминает `version_id` в `options`, но `ocr_service_api.md:49-70` его **не содержит**. Решение: `version_id` **не** передаётся в Parser/OCR (см. P12-6). Обновить `parsing_specifications.md` — удалить упоминание |
| OC-7 | Добавить примеры для каждого порога качества (reprocess, operator, auto) | 🟡 | 3 примера в `ocr_service_api.md`: (1) `avg_confidence=0.45` → `reprocess_required=true, operator_confirmation_required=true`; (2) `avg_confidence=0.72` → `reprocess_required=false, operator_confirmation_required=true`; (3) `avg_confidence=0.95, pages_failed=0` → оба `false` (автоматический путь) |

---

## 8. Converter-Validator Service (`docs/api/converter_validator_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| CV-1 | Добавить аутентификацию service-to-service | 🔴 P0-5 | mTLS/X-Service-Token |
| CV-2 | ⏳ **Отложено** — «Чёрный ящик», дождаться примеров от Семёна | 🟡 P8-10 | **Блокер**: примеры семейств конвертации **не предоставлены**. Требуется от Семёна. После получения — дополнить описание |
| CV-3 | Зафиксировать **LLM для извлечения метаданных**: `deepseek 4 flash` (**внешнее API**) | 🟠 P13-5 | (1) `POST /converter/preview/metadata` — для извлечения структурированных метаданных. (2) `app_settings.llm.{api_url,api_key,temperature=0.0,max_tokens=2048}` (для извлечения — temperature=0). (3) `system_prompt` из `prompts/converter_metadata.md` |
| CV-4 | Синхронизировать состав preview-метаданных с актуальным ответом конвертера | 🟡 | Заменить старый набор полей (`doc_code`, `title`, `document_type`, `year`, `revision`) на актуальный: `doc_code`, `title`, `mks_oks_code`, `okstu_code`, `udk_code`, `pkb_codes[]`, `document_type`, `year`, `era`, `validity_status`, `issuing_body`, `jurisdiction`, `source_type`, `language`, `title_hash_sha256` (источник — схема `schema_converter_preview.json`). Обновить пример ответа, таблицу полей, описание шага 3 и таблицу режимов в `converter_specification.md` |
| CV-5 | Добавить в конвертер принцип сохранения preview-метаданных | 🟡 | Добавить принцип «Preview-метаданные сохраняются» в `converter_specification.md`: весь JSON ответа preview фиксируется в черновике (`registry.drafts.preview_metadata`) и при approve копируется в карточку документа (`registry.documents.preview_snapshot`) для истории и аудита |

---

## 9. RAG Builder Service (`docs/api/rag_builder_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| RB-1 | Добавить аутентификацию service-to-service | 🔴 P0-5 | mTLS/X-Service-Token |
| RB-2 | Описать смену модели эмбеддингов (ALTER TABLE) | 🔵 P4-9 | SQL для перестроения индекса при смене модели |
| RB-3 | Описать repromote/переиндексацию | 🔵 P4-10 | Полный процесс: очистка → перестроение чанков |
| RB-4 | Зафиксировать **default chunk_size=1024** (вместо текущего `semantic_512`) | 🟠 P13-1 | Сейчас `:52, 119` default = `semantic_512` (≤512 токенов). Новый default: `chunk_strategy=fixed_1024` (P13-1). `semantic_512` и `fixed_256` оставить как альтернативы через `options.chunk_strategy` |
| RB-5 | Зафиксировать **default embedding_dim=2048** (Qwen3-Embedding-4B, **внешнее API**) | 🟠 P13-1 | `db_diagrams.md:448` — `VECTOR(1536)` константа (D10). Новый default: `VECTOR(2048)` с возможностью выбора 1024/2048/2560/4096 через `options.embedding_dim`. **Уточнение 17.06**: Qwen3-Embedding-4B — **внешнее API** (НЕ локальный Infinity). Endpoint и API-key в `app_settings.rag.embedding_api.{url,key}`. Зависимость от внешнего сервиса фиксировать в `service_dependencies.md` |

---

## 10. RAG Search Service (`docs/api/rag_search_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| RS-1 | Добавить аутентификацию service-to-service | 🔴 P0-5 | mTLS/X-Service-Token |
| RS-2 | API аудит (закрыть B7a) | 🟠 P1-1 | Проверить полноту |
| RS-3 | Зафиксировать 9 стратегий поиска S1–S9 | 🟡 P4-4 | Описать все стратегии, не только гибридный поиск |
| RS-4 | Описать таймауты и метрики производительности | 🟡 P4-5 | Поиск ≤500 мс |
| RS-5 | Зафиксировать итоговую конфигурацию | 🟡 P4-6 | Модель, top_k, RRF k, режим эмбеддингов |
| RS-6 | Сделать **Vector+Rerank (S2)** единственным production-режимом; убрать BM25/Hybrid из `search_type` для прод | 🔴 P13-2, P13-3 | Сейчас `:34-56` `search_type: hybrid\|sparse\|dense`. Новый контракт: `search_type: vector_rerank` (default, прод) и `search_type: experimental.{sparse\|hybrid\|hybrid_rrf\|multi_query}` (для экспериментов) |
| RS-7 | Убрать упоминания `version_id` из RAG Search API (если есть) | 🟠 P12-6 | Проверить grep-ом весь файл. Должно быть: `rag_documents.doc_id` (без версии). Удалить `version_id` из `POST /rag/search` query/filters |
| RS-8 | Зафиксировать параметры Rerank: `rerank_top_n=50`, `rerank_model=bge-reranker-v2-m3-int8` (через **TEI-сервер**) | 🟠 P13-3 | Сейчас `rerank: bool` (default `true`), без модели и `rerank_top_n`. **Уточнение 17.06**: модель — `bge-reranker-v2-m3-int8` (int8-квантизация), источник — **TEI** (text-embeddings-inference, локальный), URL в `app_settings.rag.rerank_url`. **Не** внешнее API |
| RS-9 | Добавить фильтр `?valid_at=2026-06-18` (выборка документов на дату действия) | 🟠 P12-5 | `dateMax` для бессрочных = `'infinity'::date` в PG (см. RG-6). Если `valid_at IS NULL` — без фильтра (текущее поведение). Если указан — добавлять в WHERE: `valid_from <= valid_at AND valid_until >= valid_at`. Фильтрация бессрочных работает автоматически |
| RS-10 | Зафиксировать **LLM для генерации ответа**: `deepseek 4 flash` (**внешнее API**) | 🟠 P13-5 | (1) В `POST /rag/search` (Query Service) — для генерации финального ответа. (2) `app_settings.llm.{api_url,api_key,temperature=0.2,max_tokens=1024,top_p=0.95}`. (3) `system_prompt` из `prompts/rag_system.md`. (4) **Уточнение 17.06**: deepseek 4 flash — **внешнее API**, не локальный inference |

---

## 11. Analyse Service (`docs/api/analyse_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| AN-1 | Добавить аутентификацию service-to-service | 🔴 P0-5 | mTLS/X-Service-Token |
| AN-2 | Исправить `reference_document` с string на bigint | 🟡 D18 | Строка 164: `"doc-norm-001"` → bigint |

> ⚠️ Сервис заморожен. Только критические изменения.

---

## 12. Integration Service (`docs/api/integration_service_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| IN-1 | Добавить аутентификацию service-to-service | 🔴 P0-5 | mTLS/X-Service-Token |
| IN-2 | Заменить `uploaded_at` на `created_at` (timestamptz) | ⚪ D20 | Строки 54, 64. P2-10 унификация нейминга |

> ⚠️ Интеграции не в MVP. Только критичные изменения.

---

## 13. Common API (`docs/api/common_api.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| CM-1 | Восстановить RBAC-вкладки search/registry. **`checks` убрать** (нет API, решение 03.06) | 🟠 P1-2 | Матрица обновлена ✅. `checks` не нужен — API нет. Сверить с `docs_ui_compare.md` #1, #3, #7 |
| CM-2 | Rate limiting — синхронизировать с Gateway | 🟠 P3-1 | Документация честная: `common_api.md:389-392` с пометкой «⏳ требует реализации». Таблица лимитов есть (строки 395-407). **Проблема в коде**, не в документации |
| CM-3 | IDOR — добавить примечание | 🟠 P3-3 | rate-limit и audit для идентификаторов |
| CM-4 | Добавить service-to-service auth в раздел аутентификации | 🔴 P0-5 | Расширить секцию «Аутентификация» |
| CM-5 | Создать раздел «Структурированное логирование» с обязательными полями | 🟠 P11-1, P11-2, P11-3 | Поля: `timestamp, level, service, trace_id, span_id, request_id, user_id, path, method, status, latency_ms`. Уровни (P11-3) + правила эскалации. Корреляционные заголовки (P11-2): `X-Request-ID`, `X-Trace-ID`, `X-Draft-ID`, `X-Document-ID`, `X-Version-ID` |
| CM-6 | Создать раздел «OpenTelemetry + service_checker» — обязательная инструментация всех сервисов | 🟠 P11-5, P11-9 | (1) OTEL SDK инициализируется в `main.py` каждого сервиса (`OTLPSpanExporter` → `signoz-otel-collector:4317`); (2) обязательные span-атрибуты: `service.name`, `service.version`, `deployment.environment`; (3) **`service_checker`** (отдельный инструмент) — автопроверка в CI: (a) OTEL SDK настроен; (b) OTLP-экспорт в SigNoz работает; (c) span-атрибуты присутствуют; (d) структура логов корректна; (e) correlation-id пробрасывается. Запуск: `service_checker <service_name> --post-deploy` |

---

## 14. Database / DDL (`docs/database/db_diagrams.md`)

### Изменения в существующем файле

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| DB-1 | Исправить формулу title_hash_sha256 (строки 301, 388) | 🔴 P0-1 | Заменить 3-польную на 6-польную: `era \| source_type \| mks_oks_code \| okstu_code \| doc_code \| normalized_title` |
| DB-2 | Добавить CHECK/ENUM на source_type, document_type, era, validity_status, jurisdiction, processing_status | 🟡 P2-1 | DDL — ALTER TABLE … ADD CHECK |
| DB-3 | file_hash_sha256, title_hash_sha256 → CHAR(64) | 🟡 P2-2 | В ER-диаграмме и DDL |
| DB-4 | UNIQUE: (doc_code, era), (document_id, path), (document_id, version_number), (section_id, chunk_index), chat.projects.code | 🟡 P2-3 | DDL |
| DB-5 | ON DELETE/ON UPDATE для всех FK | 🟡 P2-4 | По матрице из db_audit_report.md |
| DB-6 | Soft-delete: deleted_at TIMESTAMPTZ | 🟡 P2-5 | Добавить в registry.documents, chat.sessions, chat.projects |
| DB-7 | Доп. индексы | 🟡 P2-6 | doc_code (B-tree), path (GiST ltree), target_doc_code, (session_id, created_at), частичные по FSM |
| DB-8 | CHECK на положительность счётчиков | 🟡 P2-7 | chunk_count, processing_time_ms, file_size_bytes, version_number |
| DB-9 | CHECK на mks_oks_code/okstu_code | 🟡 P2-8 | Соответствие classifier_registry |
| DB-10 | Триггер синхронизации document_chunks.document_id | 🟡 P2-9 | Связать с section.document_id |
| DB-11 | Унифицировать нейминг | 🟡 P2-10 | uploaded_at→created_at, uploaded_by→created_by, и т.д. |
| DB-12 | Исправить document_versions.uploaded_at/uploaded_by (строки 86-87) | 🟡 D3 | Остались не унифицированными |
| DB-13 | Исправить text→CHAR(64) для hash-полей (строки 31, 81) | 🟡 D7 | В ER-диаграмме |

### Новые файлы

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| DB-14 | Создать docs/database/ddl_migrations_<дата>.md | 🟡 P2-11 | Скрипты ALTER TABLE для всех DDL-изменений |
| DB-15 | Сделать размерность embedding параметром, а не константой 1536 | 🟡 D10 | `db_diagrams.md:448` — VECTOR(1536) жёстко. Модель может быть Qwen3 с 2560/2048/1536 |
| DB-16 | Добавить поле `preview_snapshot` (jsonb, nullable) в ER-диаграмму `registry.documents` | 🟡 | Поле хранит исходный JSON ответа Converter-validator preview, копируется из `registry.drafts.preview_metadata` при approve. Обновить ER-диаграмму и примечания §0 (черновики: указать, что `preview_metadata` копируется) и §1 (реестр: описать `preview_snapshot`). Уточнить, что поле не участвует в поиске — только для истории |
| DB-17 | Переписать `ddl_migrations_17_06.md` в описательный стиль | 🟡 | Заменить SQL-блоки на таблицы и списки. CHECK-ограничения, внешние ключи, индексы — в виде таблиц. Добавить описание `preview_snapshot`. Закрепить стиль в `guide.md` (правило: документация без SQL-кода) |

---

## 15. Pipelines

### 15.1. Pipeline 1 — Формирование (`docs/pipelines/pipeline1-formation.md`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| P1F-1 | Описать компенсацию при дубликате после approve | 🔴 P0-3 | Неатомарность `check-uniqueness`. Решение: `INSERT … ON CONFLICT` + уникальный индекс |
| P1F-2 | Добавить триггер `review_required → validation` | 🟠 P1-20 | **Не описан**. grep по всем pipeline-файлам — `review_required` нигде не найден. `specificity.md` LP-V8: ⬜ открыто |
| P1F-3 | Проверить актуальность pipeline1-formation_detail.md | 🟡 D1 | Возможно, требует синхронизации |
| P1F-4 | Добавить шаг 3.0 — копирование preview-слепка при создании карточки документа | 🟡 | В таблицу процесса Этапа 3 (Registry) добавить шаг 3.0: Registry копирует `preview_metadata` из `registry.drafts` в `preview_snapshot` карточки документа (`registry.documents`). Шаг выполняется перед сохранением карточки (до шага 3.1) |

### 15.2. Pipeline 2 — Индексация (`docs/pipelines/pipeline2-indexation.md`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| P2I-1 | Удалить или описать partially_indexed | 🟠 P1-16 | Строка 145: упоминается, но не входит в FSM |
| P2I-2 | Описать механизм `indexed → failed : Integrity check failed` | 🟠 P1-17 | Детально: какой триггер, алгоритм проверки |
| P2I-3 | Устранить противоречие компенсации (транзакция vs cleanup) | 🟠 P1-18 | Стр. 72: «откат транзакции» vs стр. 160: «удалить чанки» |
| P2I-4 | Зафиксировать 9 стратегий поиска | 🟡 P4-4 | S1–S9 из rag_experiments_methodology.md |
| P2I-5 | Описать таймауты индексации | 🟡 P4-5 | CPU ≤60 с / GPU ≤15 с |
| P2I-6 | Зафиксировать конфигурацию индексации | 🟡 P4-6 | Модель, размерность, batch |

### 15.3. Pipeline 3 — Поиск (`docs/pipelines/pipeline3-search.md`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| P3S-1 | Добавить таймаут pending 30 с | 🟠 P1-15 | Сейчас только 48ч абсолютный таймаут. Нужен 30с + 48ч |
| P3S-2 | Согласовать таймаут pending | 🟡 D4 | В плане 30с, в коде 48ч. Устранить расхождение |
| P3S-3 | Исправить `section_id:sec-4.2` на bigint в примерах | 🟡 D19 | Строки 141, 150: `sec-4.2` → `420042` |

### 15.4. Overview (`docs/pipelines/overview.md`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| OV-1 | Добавить SigNoz в описание мониторинга (OpenTelemetry + ClickHouse, Docker-сеть signoz-network) | 🟡 P8-8 | Victoria Metrics уже не упоминается. SigNoz не добавлен. Конкретика в обсуждениях 08.06, 16.06 |
| OV-2 | Добавить Gateway в таблицу сервисов | 🟡 D8 | `overview.md:517-529` — Gateway отсутствует в сводной таблице. В README есть |
| OV-3 | Исправить CAS-путь в overview.md | 🟡 D9 | `overview.md:248` — `{hash}.ext` с расширением, а спецификация CAS — без расширения. Устранить противоречие |
| OV-4 | Описать **5 шагов внедрения SigNoz** в новый раздел «Мониторинг» | 🟠 P11-5 | 5 шагов из обсуждения 16.06 (микросервисы): (1) поднять SigNoz в Docker-сети `signoz-network`; (2) добавить OTEL SDK в зависимости; (3) стандартизировать span-имена; (4) экспорт OTLP; (5) дашборды. Стандартные span-имена перечислить |
| OV-5 | Зафиксировать **SLO/SLI и алерты** в разделе «Мониторинг» | 🟡 P11-7, P11-8 | SLO: RAG Search p95 ≤ 500 мс (recall ≥ 0.85), индексация p95 ≤ 60 с CPU / ≤ 15 с GPU, Parser/OCR success ≥ 99 %, Gateway 5xx ≤ 0.1 %. Алерты: 6 правил из P11-8 |

---

## 16. Specifications

### 16.1. Parser Specifications (`docs/specifications/parsing_specifications.md`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| SP-1 | Обновить контракт — схлопывание /preview + /process | 🟡 P5-4 | Решение от 08.06. Документ в старом формате |
| SP-2 | Зафиксировать риск Lama Parser (облако) | 🟡 P3-6 | Для конфиденциальных документов |
| SP-3 | Синхронизировать под единый POST /process | 🟡 D6 | Дубликат SP-1, перекрёстная ссылка |
| SP-9 | Описать оценку качества текстового слоя PDF | 🔵 D12 | Функция в shared/, пороги, градации (OCR/подтверждение/авто). Решение гр1 16.06 |

### 16.2. Normalizer Specification (`docs/specifications/normalizer_specification.md`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| SP-4 | Добавить раздел про нормализатор doc_code для резолвера | 🔵 P5-8 | Единый нормализатор для Converter-validator и Registry |

### 16.3. CAS Storage Specification (`docs/specifications/cas_storage_specification.md`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| SP-5 | Синхронизировать с решением 08.06 (два бакета: `files` + `images`, SHA-256 без расширения) | 🟡 P5-9 | Сейчас: один бакет, путь `{doc_id}/v{version}/{hash}`. Решение: два бакета, SHA-256 без расширения |

### 16.4. Purgatory Scenario (`docs/specifications/purgatory_scenario.md`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| SP-6 | Обновить состав сервисов | ✅ P5-5 | ✅ Актуален. Сценарии А–Е с текущими именами сервисов |

### 16.5. CSV-файлы (`docs/specifications/*.csv`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| SP-7 | Добавить seed с mks_oks_code: 47.020 | 🟡 P5-7 | **Нет в CSV**. grep по обоим CSV-файлам — 47.020 не найден. Seed не добавлен |

### 16.6. Новые specification-файлы

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| SP-8 | Создать registry_resolver_spec.md | 🔴 P0-4 / 🔵 P5-10 | Спецификация резолвера графа связей. Черновик в `analyse_alternative_project.md §1.1` |

---

## 17. Glossary (`docs/glossary.md`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| GL-1 | Добавить preview_not_supported | 🟡 P5-3 | Флаг режима preview |
| GL-2 | «артефакт» → «результат обработки» | ⚪ P5-2 | Термин встречается в glossary.md:54 |
| GL-3 | Добавить ссылку на справочник ПКБ | 🔵 P10-3 | В раздел «Классификаторы и коды» |
| GL-4 | Добавить ui-state, query-cache, optimistic-update, timeline, editor-dirty | 🟡 P5-3 | 5 терминов из UI-контракта |
| GL-5 | Добавить **`dateMax`** — символическая константа «бессрочный документ» | 🟠 P12-5 | В PostgreSQL реализуется как `'infinity'::date` (нативное значение, корректно работает в B-tree индексах и сравнениях). Альтернатива: `'9999-12-31'`. Семантика: документ действует от `valid_from` до отмены (нет конечной даты действия). Используется в `validity_dates_spec.md` (NEW-16) |

---

## 18. README / Navigation (`docs/README.md`)

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| RD-1 | Синхронизировать описание экрана «Загрузка документа» | 🟡 P6-2 | Сверить с docs_ui.md |
| RD-2 | Синхронизировать описание «Базы знаний» | 🟡 P6-3 | Сверить с docs_ui.md |
| RD-3 | Добавить секцию «Open Questions» | 🔵 P6-4 | Ссылка на specificity.md A25–A37 |
| RD-4 | Обновить дерево файлов (после создания новых директорий) | 🟡 P6-1 | Добавить docs/methodology/, docs/architecture/, docs/ui/ |

---

## 19. Docs_plans / Archive

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| DP-1 | Перенести docs_plans/audit/*.md в docs/audit/ | 🔵 P7-2 | Каноническое место |
| DP-2 | Перенести docs_plans/docs_ui.md и docs_ui_compare.md в docs/ui/ | 🔵 P6-6 | |
| DP-3 | Перекодировать имена обсуждений в UTF-8 | ⚪ P7-3 | |
| DP-4 | Переименовать файлы обсуждений в семантические | ⚪ P7-4 | |
| DP-5 | Удалить дубль ui-final-… (28 КБ) | ⚪ P7-5 | |
| DP-6 | Создать docs_plans/discussions/README.md | 🔵 P7-6 | Индекс встреч с резюме |
| DP-7 | Добавить front-matter в обсуждения | 🔵 P7-7 | Дата, участники, решения, статус |
| DP-8 | Добавить cross-references specificity ↔ обсуждения | 🔵 P7-8 | |
| DP-9 | Добавить плашку «исторический документ» | ⚪ P5-6 | В pipeline1-formation_discussion.md |

---

## 20. Новые файлы (создать с нуля)

| # | Файл | Приоритет | Источник / Содержание |
|---|------|-----------|----------------------|
| NEW-1 | `docs/specifications/registry_resolver_spec.md` | 🔴 P0-4 | Спецификация резолвера. Черновик в `analyse_alternative_project.md §1.1` |
| NEW-2 | `docs/database/ddl_migrations_<дата>.md` | 🟡 P2-11 | Скрипты ALTER TABLE |
| NEW-3 | `docs/plans/quality_report_sprint2.md` | 🟡 P4-1 | Шаблон/заготовка отчёта |
| NEW-4 | `docs/methodology/` (каталог) | 🟡 P4-2 | Перенести rag_experiments_methodology.md и rag_evaluation_methodology.md |
| NEW-5 | `docs/ui/` (каталог) | 🔵 P6-6 | Перенести docs_ui.md и docs_ui_compare.md |
| NEW-6 | `docs/architecture/service_dependencies.md` | 🔵 P9-1 | Перечень Python-пакетов (из Ошибки запуска сервисов.md) |
| NEW-7 | `docs/deployment.md` | 🔵 P8-6 | Деплой UI + Gateway на сервер |
| NEW-8 | `docs/CHANGELOG.md` | 🔵 P6-5 | История изменений документации |
| NEW-9 | `docs_plans/discussions/README.md` | 🔵 P7-6 | Индекс встреч |
| NEW-10 | `docs/specifications/pkb_domains_classifier.csv` | 🔵 P10-2 | Таблица из справочника ПКБ |
| NEW-11 | `docs/audit/audit_<дата>.md` (повторный) | 🟡 P7-9 | Аудит после закрытия P0 |
| NEW-12 | `docs/audit/ui_admin_questions_2026-06_16.md` | 🟡 D15 | Вопросы к backend по UI Admin (Q1–Q5) |
| NEW-13 | `docs/api/frontend_types.md` | 🟡 D16 | UI-типы: UIStore, QueryCache, OptimisticUpdate, DocumentTimelineEvent, MetadataEditorState |
| NEW-14 | `docs/specifications/quality_thresholds_spec.md` | 🔴 P12-2 | Спецификация 2 порогов качества: (1) `reprocess_required` — парсер/OCR решает, что нужна доп. обработка; (2) `operator_confirmation_required` — нужна ручная проверка. Поля `issues[]`. **Тесты** для проверки качества: (a) unit-тесты на функцию `assess_quality(confidence, pages_failed, lama_fallback)` в `shared/` — 3 кейса (reprocess, operator, auto); (b) интеграционные тесты на эндпоинт Parser/OCR с 5 эталонными PDF (высокое качество, низкое качество, рукопись, сканирование, пустой PDF); (c) property-based тесты: монотонность — при снижении confidence флаги должны срабатывать в порядке `reprocess → operator → none` |
| NEW-15 | `docs/architecture/monitoring.md` | 🟠 P11-5, P11-6, P11-7, P11-8 | Разделы: (1) архитектура SigNoz + OTEL + ClickHouse; (2) span-имена (стандарт); (3) health/ready vs live для всех сервисов; (4) SLO/SLI; (5) алерты. **Тесты**: (a) интеграционный тест OTEL — генерируется span с правильными атрибутами при вызове эндпоинта; (b) тест алертов — mock Prometheus, проверка firing правил |
| NEW-16 | `docs/specifications/validity_dates_spec.md` | 🟠 P12-5 | Спецификация полей `valid_from` / `valid_until` / **`dateMax`**: (1) DDL — `valid_from DATE NOT NULL`, `valid_until DATE NOT NULL DEFAULT 'infinity'::date`, CHECK `valid_until >= valid_from`, INDEX `(valid_from, valid_until)`; (2) **`dateMax` — символическая константа**, в PostgreSQL = `'infinity'::date` (нативное значение, корректно работает в B-tree и сравнениях). Альтернатива для не-PG-клиентов: `'9999-12-31'`; (3) API-контракт — оба поля обязательные, фильтр `?valid_at=YYYY-MM-DD` через `valid_from <= d AND valid_until >= d`; (4) семантика `dateMax` — «бессрочный документ», действует от `valid_from` до отмены; (5) **тесты**: (a) SQL-тест T-6 — 4 документа (конечный, конечный, бессрочный, истёкший), 4 даты; (b) API-тест: `GET /registry/documents?valid_at=2050-01-01` возвращает только бессрочный |
| NEW-17 | `docs/specifications/draft_to_version_spec.md` | 🟠 P12-4 | Связь черновик → документ/версия. Контракт `is_new_document` + `document_id` + `version_id` в черновике. Поток: `POST /drafts` → preview → decide(approve) → `POST /registry/documents` с `source_draft_id` → ответ содержит `version_id` → записать в `registry_drafts.version_id`. **Тесты**: (a) happy-path: новый документ, новая версия, цепочка `draft→document→version`; (b) новая версия существующего: `is_new_document=false`, `version_id=N+1`; (c) реджект: `version_id IS NULL` |
| NEW-18 | `docs/architecture/opentelemetry_spec.md` | 🟠 P11-5, P11-9 | Спецификация OpenTelemetry: (1) список обязательных зависимостей (`opentelemetry-api`, `opentelemetry-sdk`, `opentelemetry-exporter-otlp`, `opentelemetry-instrumentation-{fastapi,sqlalchemy,httpx}`); (2) snippet инициализации в `main.py` (для всех 12 сервисов); (3) стандартные span-имена (`http.{method}.{route}`, `db.{op}.{table}`, `parser.process`, `rag.search`, `converter.extract_metadata`); (4) обязательные атрибуты ресурса; (5) **секция `service_checker`**: команды запуска в CI и post-deploy, exit-коды, формат отчёта |

---

## 21. Аудит и проверка

| # | Задача | Приоритет | Описание |
|---|--------|-----------|----------|
| AUD-1 | Запустить повторный аудит по check_rule.md после закрытия P0 | 🟡 P7-9 | Новый audit_*.md |
| AUD-2 | Актуализировать Excel-таблицу статусов сервисов | 🔵 P7-10 | Внешний артефакт |
| AUD-3 | Решить судьбу encoding_issues_report.txt | ⚪ D2 | В корне docs/ |

---

## 22. Тесты качества (добавлено 17.06, раздел 5-й)

> Источник: гр1 16.06 (качество), 17.06 (тесты), P12-2, NEW-14. Тесты привязаны к спецификациям, а не к сервисам.

| # | Задача | Приоритет | Описание | Связанный файл |
|---|--------|-----------|----------|----------------|
| T-1 | Unit-тесты `assess_quality(confidence, pages_failed, lama_fallback)` в `shared/` | 🔴 P12-2 | 3 кейса: (1) `0.45, 2, false` → `reprocess_required=true, operator_confirmation_required=true`; (2) `0.72, 0, true` → `reprocess=false, operator=true`; (3) `0.95, 0, false` → оба `false`. Параметризовать пороги (значения из `app_settings`) | `tests/shared/test_quality.py` |
| T-2 | ⏳ **Отложено** — Интеграционные тесты `POST /parser/process` и `POST /ocr/process` с 5 эталонными PDF | 🟠 P12-2 | **Блокер**: 5 эталонных PDF **не предоставлены**. Требуется от владельца данных. После получения: (1) high_quality.pdf; (2) low_confidence.pdf; (3) handwritten.pdf; (4) scanned_300dpi.pdf; (5) empty.pdf. Проверка: правильные значения `reprocess_required` и `operator_confirmation_required`, наличие `issues[]` для проблемных кейсов | `tests/parser/test_process_quality.py`, `tests/ocr/test_process_quality.py` |
| T-3 | Property-based тесты монотонности порогов | 🟡 P12-2 | Использовать `hypothesis`. Утверждение: при снижении `confidence` от 1.0 до 0.0 флаги должны срабатывать в порядке `none → operator → reprocess` (т.е. если `reprocess=false` и `operator=true` — увеличение confidence должно убрать оба флага, но не должно снять `reprocess=true` и оставить `operator=true`) | `tests/shared/test_quality_props.py` |
| T-4 | Тест записи `issues[]` в БД (`pipeline.draft_issues`) | 🟠 P12-3 | После прогона Parser/OCR — запись в БД содержит все `issues[]` с правильным `draft_id`, `code`, `severity`, `location`. Чтение через `GET /drafts/{id}/issues` | `tests/orchestrator/test_issues_recording.py` |
| T-5 | Тест передачи `draft_id` в Parser/OCR | 🔴 P12-1 | (a) `POST /parser/process` без `draft_id` → `400 BAD_REQUEST: DRAFT_ID_REQUIRED`; (b) с `draft_id=99999` (не существует) → `404 DRAFT_NOT_FOUND`; (c) с валидным `draft_id` — задача стартует, в логах присутствует `X-Draft-ID` | `tests/orchestrator/test_draft_id_propagation.py` |
| T-6 | SQL-тест выборки документов на дату (`valid_at`) | 🟠 P12-5 | 4 документа с разными `valid_from/valid_until` (все NOT NULL): (a) `[2025-01-01, 2027-01-01]` — конечный; (b) `[2026-06-01, 2030-12-31]` — конечный; (c) `[2024-01-01, 'infinity'::date]` — **бессрочный** (`dateMax`); (d) `[2018-01-01, 2020-01-01]` — истёкший. Запрос `SELECT … WHERE valid_from <= :d AND valid_until >= :d`. Проверить: (1) `valid_at=2019-01-01` — только (d); (2) `valid_at=2025-06-01` — (a) и (c); (3) `valid_at=2030-01-01` — (b) и (c); (4) `valid_at=2050-01-01` — только (c) благодаря `dateMax='infinity'::date` | `tests/registry/test_validity_filter.sql` или `tests/registry/test_validity_filter.py` |
| T-7 | Тест связки draft → document → version | 🟠 P12-4 | Сценарий: (a) `POST /drafts` → approve → `POST /registry/documents?source_draft_id=…` → ответ содержит `version_id`; (b) `GET /drafts/{id}` показывает `document_id` и `version_id`; (c) реджект черновика → `version_id IS NULL` | `tests/integration/test_draft_to_version.py` |
| T-8 | Тест RAG Search: default конфигурация (Qwen 4B, dim 2048, chunk 1024, Vector+Rerank S2) | 🟠 P13-1 | (a) `POST /rag/search` без параметров → стратегия `vector_rerank`; (b) топ-1 результат соответствует эталонному (golden dataset); (c) при `search_type=experimental.sparse` → явное предупреждение в логах (флаг экспериментальный) | `tests/rag/test_default_config.py` |
| T-9 | Тест Rerank: `rerank_top_n=50` действительно берёт топ-50 по embedding и сортирует по rerank | 🟡 P13-3 | Проверка: при `top_k=10, rerank_top_n=50` — отранжированные результаты отличаются от `top_k=10, rerank=false` | `tests/rag/test_rerank.py` |
| T-10 | Тест Registry BM25 Search: `GET /registry/search?q=ГОСТ` возвращает документы с `ГОСТ` в `title` или `doc_code` | 🟠 P13-2 | (a) Запрос с точным совпадением в `title`; (b) с частичным (`гост*`); (c) с учётом `classifier_links`. **Не должен** вызывать Vector-поиск | `tests/registry/test_search_bm25.py` |
| T-11 | Тест корреляционных заголовков (`X-Request-ID`, `X-Trace-ID`, `X-Draft-ID`) | 🟠 P11-2 | (a) Запрос **без** `X-Request-ID` — Gateway генерирует UUIDv4; (b) с заголовком — пробрасывается дальше; (c) все логи запроса содержат одинаковый `request_id`; (d) span в OTEL связан с этим `request_id` | `tests/integration/test_correlation.py` |
| T-12 | Тест health/ready vs health/live | 🟡 P11-6 | (a) `GET /health/ready` возвращает 200, только если БД, MinIO, Infinity доступны; (b) при недоступности MinIO — `503`; (c) `GET /health/live` всегда 200, пока процесс жив | `tests/integration/test_health.py` |
| T-13 | Тест алертов (P11-8) | 🟡 P11-8 | Mock Prometheus + алерт-правила. Проверка: 6 правил (5xx, RAG p95, failed, indexed→failed, pending_index, MinIO disk) firing при пороговых значениях | `tests/monitoring/test_alerts.py` |
| T-14 | Тест **`service_checker`** (P11-9) | 🟠 P11-9 | (a) Запуск `service_checker <service>` на эталонном сервисе — exit-code 0; (b) на сервисе без OTEL — exit-code 1 + понятный отчёт; (c) на сервисе с битым OTLP-эндпоинтом — exit-code 2 + указание на проблему; (d) проверка наличия обязательных span-атрибутов (`service.name`, `service.version`, `deployment.environment`) | `tests/observability/test_service_checker.py` |
| T-15 | Интеграционный тест OTEL → SigNoz | 🟠 P11-5 | (a) Старт сервиса в test-среде; (b) вызов эндпоинта; (c) проверка, что span отправлен в mock OTLP-collector (OTLP-test-server); (d) span содержит правильные атрибуты (см. T-14); (e) корреляция с `X-Request-ID` сохранена | `tests/observability/test_otel_export.py` |

---

## Приложение: Матрица ответственных

| Сервис/Компонент | Файл(ы) | Кол-во задач | Критичные (🔴) |
|-----------------|---------|:---:|:---:|
| Gateway Service | `gateway_service_api.md` | 10 | 1 (GW-1) + GW-9, GW-10 (P11-2/5/9) |
| Registry Service | `registry_service_api.md` | 9 | — (RG-1, RG-6, RG-7, RG-8 — P12-4/5/13-2) |
| Query Service | `query_service_api.md` | 5 | 1 (QS-1) |
| Orchestrator Service | `orchestrator_service_api.md` | 8 | 1 (OR-5 — P12-1) + OR-6, OR-7, OR-8 (P12-3/4, P11-5/9) |
| Auth Service | `auth_service_api.md` | 2 | — |
| Parser Service | `parser_service_api.md` | 4 | 2 (PS-1, PS-3) + PS-4 (P12-2/3) |
| OCR Service | `ocr_service_api.md` | 7 | 2 (OC-1, OC-4) + OC-5 (P12-2/3) |
| Converter-Validator | `converter_validator_service_api.md` | 3 | 1 (CV-1) + CV-3 (P13-5) |
| RAG Builder | `rag_builder_service_api.md` | 5 | 1 (RB-1) + RB-4, RB-5 (P13-1) |
| RAG Search | `rag_search_service_api.md` | 10 | 3 (RS-1, RS-6, RS-7) + RS-8, RS-9, RS-10 (P13-3/5, P12-6, P12-5) |
| Analyse Service | `analyse_service_api.md` | 1 | 1 (AN-1) |
| Integration Service | `integration_service_api.md` | 1 | 1 (IN-1) |
| Common API | `common_api.md` | 6 | — (P3-1 — код) + CM-5, CM-6 (P11-1/2/3/5/9) |
| Database / DDL | `db_diagrams.md` | 14 | 1 (DB-1) + поля P12-4/5, NEW-15/16/17 |
| Pipeline 1 | `pipeline1-formation.md` | 3 | 1 (P1F-1) |
| Pipeline 2 | `pipeline2-indexation.md` | 6 | — |
| Pipeline 3 | `pipeline3-search.md` | 2 | — |
| Overview | `overview.md` | 3 | — (OV-4, OV-5 — P11-5/7/8) |
| Specifications | `parsing_specifications.md`, `normalizer_specification.md`, `cas_storage_specification.md`, `purgatory_scenario.md`, `*.csv` | 8 | — (P5-5 ✅) |
| Glossary | `glossary.md` | 3 | — |
| README | `README.md` | 4 | — |
| Docs_plans/Archive | `docs_plans/` | 9 | — |
| Новые файлы | — | 16 | 1 (NEW-1) + NEW-14/15/16/17/18 (P12-2/3/4/5, P11-5..8/9) |
| Аудит | — | 3 | — |
| **Тесты качества** | `tests/**/*.py` | 15 | 3 (T-1, T-5, T-8) + T-2, T-4, T-6, T-7, T-10, T-11, T-14, T-15 (🟠); T-2 — ⏳ отложен |
| **Итого** | | **135** | **19 🔴** (P3-1 — код, остальные — docs/API/тесты) |
