API Нейроассистента ПКБ (v1.0)
===============================

### Общие положения

- Базовый URL (оркестратор): `https://{host}/api/v1`

- Базовый URL для внутренних запросов: `https://127.0.0.1/api/v1`

- Формат данных: `application/json`, для загрузки файлов – `multipart/form-data`

- Аутентификация: все запросы, кроме `/auth/*` и `/system/health`, требуют заголовок  
  `Authorization: Bearer <access_token>`. Токен получается через `/auth/token`.

- **Формат ответа** (публичное API): все ответы оборачиваются в единую структуру:

```json
{
  "success": true,
  "data": { ... },
  "error": null
}
```

При ошибке:

```json
{
  "success": false,
  "data": null,
  "error": {
    "code": "DOCUMENT_NOT_FOUND",
    "message": "Документ не найден"
  }
}
```

Для списковых ответов `meta` содержит пагинацию:

```json
{
  "meta": {
    "total": 150,
    "page": 1,
    "page_size": 50
  }
}
```

- **Формат ошибок** (внутренние сервисы, без обёртки):

```json
{
  "error": {
    "code": 422,
    "code_name": "INVALID_FORMAT",
    "message": "Поддерживаются только PDF и изображения",
    "details": {}
  }
}
```

[Коды ответов HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status)

Коды ответа:

| code | code_name | message | Комментарий |
|------|-----------|---------|-------------|
| 200 | OK | Успех | |
| 201 | CREATED | Создан ресурс | |
| 202 | ACCEPTED | Запрос принят | Когда нет немедленного ответа |
| 400 | BAD_REQUEST | Неверные параметры | |
| 400 | VALIDATION_ERROR | Ошибка валидации полей | Registry Service |
| 401 | UNAUTHORIZED | Нет доступа — клиент не известен | |
| 403 | FORBIDDEN | Нет доступа — нет прав на ресурс | |
| 404 | NOT_FOUND | Не найдено, нет такого адреса | |
| 404 | NOT_FOUND | Не найдено, нет ресурса | |
| 404 | CLASSIFIER_NOT_FOUND | Узел классификатора не найден | Registry Service |
| 404 | TERM_NOT_FOUND | Термин не найден | Registry Service |
| 404 | DOCUMENT_NOT_FOUND | Документ реестра не найден | Registry Service |
| 409 | HAS_CHILDREN | Нельзя удалить узел с дочерними | Registry Service |
| 409 | DUPLICATE_CODE | Код классификатора уже существует | Registry Service |
| 409 | DUPLICATE_TERM | Термин уже существует | Registry Service |
| 422 | UNPROCESSABLE_FORMAT | Ошибка семантической валидации | |
| 500 | INTERNAL_SERVER_ERROR | Внутренняя ошибка сервера | |
| 501 | NOT_IMPLEMENTED | Метод не внедрён | GET и HEAD не должны возвращать этот код |

---

### Примечания по реализации

- Типы документов строго фиксированы: normative, archival_scan, drawing, specification. Именно эти значения ожидаются в полях document_type.
  
- Обработка полным документом: API не содержит методов для ручного выделения областей. Распознавание запускается для всего документа сразу после загрузки; пользователь не может отметить фрагмент для OCR. Просмотр страниц возможен только в режиме чтения.
  
- Пороговые значения confidence: система помечает страницы с низким качеством OCR и логирует их (UC-02, UC-09). Конкретные пороги настраиваются на уровне сервисов.
  
- Трассируемость ответов: все ответы поиска и вопросно-ответной системы обязательно содержат document_id и page_number. Прямая ссылка на страницу формируется согласно публичному API просмотра (/documents/{doc_id}/pages/{page_num}).
  
- Дисклеймер о необходимости инженерной верификации присутствует в ответах /validate/compare.
  
- Все эндпоинты синхронны и возвращают результат сразу, либо возвращают идентификатор задачи для опроса статуса (сопоставление, обработка).
