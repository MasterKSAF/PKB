# 23.05.26 — Исполнительный план приведения документации

> **Основание**: [23_05_26.md](23_05_26.md) — анализ расхождений между эталоном `pipeline1-formation_detail.md` и остальной документацией.

---

## Порядок выполнения

```
ЭТАП 1 (эталон) → ЭТАП 2 (архитектура) → ЭТАП 3 (API) → ЭТАП 4 (JSON/спеки) → ЭТАП 5 (периферия)
                         │
                    ЭТАП 2 и ЭТАП 3 можно частично параллелить внутри каждого блока
```

---

## Этап 1: Правки эталонного файла

> **Зависимости**: решения по открытым вопросам из `23_05_26.md` (раздел 7).

### 1.1 `docs/pipelines/pipeline1-formation_detail.md`

| # | Правка | Где | Что сделать |
|---|--------|-----|-------------|
| 1.1a | Удалить «висячий» bullet | Раздел 3.3, между строками про protected spans и «Подготавливает плоский JSON» | Удалить пустой пункт `- ` |
| 1.1b | Уточнить «сегментирование» vs «чанкование» | Раздел 3.3 (Registry) | Заменить «разбивает документ на логические секции сегменты» на «разбивает документ на логические **секции** (записи `nsi_document_sections`)». Убрать упоминание «чанков». |
| 1.1c | Уточнить «чанкование» | Раздел 3.4 (RAG Builder) | Заменить «разбивает документ на логические секции чанки» на «разбивает **секции** (полученные от Registry) на более мелкие **чанки** для эмбеддингов» |
| 1.1d | Protected spans — решить дублирование | Разделы 3.3, 3.4 | Оставить упоминание только в RAG Builder (3.4), из Registry (3.3) убрать. Если protected spans должны влиять и на секции — добавить явное пояснение. |
| 1.1e | Уточнить место хранения preview-данных | Раздел 4 | Добавить явное указание: «Preview-данные (частичный JSON, первичные метаданные, кандидаты в дубликаты) хранятся в Оркестраторе как блок данных в журнале пайплайна и не сохраняются в БД.» |

---

## Этап 2: Архитектурные документы (pipeline-файлы + overview)

> **Зависимости**: Этап 1 завершён.

### 2.1 `docs/pipelines/pipeline1-formation.md` — **ПЕРЕПИСАТЬ**

| # | Правка | Где (строки) | Что сделать |
|---|--------|--------------|-------------|
| 2.1a | Обновить заголовок и назначение | ~L1-5 | Отразить двухфазность: «Пайплайн 1: Формирование документа (двухфазный: preview → решение → full)» |
| 2.1b | Заменить mermaid-диаграмму | ~L7-37 | Две фазы вместо одной: Фаза 1 (Preview): UI→Orch→OCR/Parser(preview)→Converter-validator(preview)→UI(решение). Фаза 2 (Full): UI→Orch→OCR/Parser(full)→Converter-validator(full)→Registry→RAG Builder |
| 2.1c | Разделить «Этап 1: Parsing» | ~L40-80 | Создать два подраздела: «OCR-сервис» и «Parser-сервис». Для каждого — таблица шагов, единый контракт выходных данных. Добавить preview-режим (max_pages, без fileKey). Убрать шаги построения структуры/заголовков (→ в Converter-validator). |
| 2.1d | Заменить «Этап 2: Validation» | ~L82-110 | Переименовать в «Этап 2: Converter-validator». Два подраздела: «Preview API» и «Полный режим». Добавить LLM, иерархию, перекрёстные ссылки. |
| 2.1e | Обновить «Этап 3: Registry» | ~L112-130 | Уточнить: сегментация на секции (не чанки). Подготовка плоского JSON для RAG Builder. |
| 2.1f | Добавить пример preview-трансформации | ~L132-155 | Третий пример: «Preview-фаза: сырой JSON (N стр.) → preview-метаданные + кандидаты в дубликаты» |
| 2.1g | Обновить FSM | ~L157-180 | Добавить состояния: `previewing`, `awaiting_decision`. Переходы: `uploaded → previewing → awaiting_decision → parsing | duplicate | new_version` |
| 2.1h | Обновить таблицу ошибок | ~L182-210 | Добавить строки: «Preview OCR/Parser — ошибка распознавания → статус preview_failed», «Preview Converter-validator — ошибка уникальности → awaiting_decision с флагом ошибки» |
| 2.1i | Обновить таблицу таймаутов | ~L212-230 | Добавить: OCR preview — 60с, Parser preview — 30с, Converter preview — 15с |

### 2.2 `docs/pipelines/overview.md` — **СУЩЕСТВЕННО ОБНОВИТЬ**

| # | Правка | Где | Что сделать |
|---|--------|-----|-------------|
| 2.2a | Обновить mermaid data flow | ~L10-38 | Добавить preview-ветку: `MinIO → OCR/Parser(preview) → Converter-validator(preview) → UI(decision)`. Показать точку ветвления. |
| 2.2b | Обновить «Сводную таблицу доступа к БД» | ~L55-71 | Добавить строки для preview-этапов: «Preview OCR/Parser — Нет», «Preview Converter-validator — Читает (Registry)» |
| 2.2c | Обновить «Матрицу ответственности сервисов» | ~L81-101 | Заменить старые названия: Parsing/OCR → OCR + Parser (две строки), Validation → Converter-validator, RAG → RAG Builder + RAG Search. Добавить Orchestrator: «Preview-фаза, хранение preview-данных» |
| 2.2d | Обновить «Эндпоинты внутренних сервисов» | ~L101-119 | Новая таблица портов: 8081 Orch, 8082 Auth, 8083 Query, 8084 Registry, 8085 Integration, 8086 Converter-validator, 8087 Parser, 8088 OCR, 8089 Analyse, 8090 RAG Builder, 8091 RAG Search |
| 2.2e | Обновить «Поток данных (Data Flow)» | ~L119-188 | Добавить preview-поток и точку решения пользователя |
| 2.2f | Обновить «Ключевые архитектурные решения» | ~L188-202 | Добавить: «Двухфазный пайплайн с user decision point», «Preview-данные в журнале Оркестратора», «OCR и Parser — независимые сервисы с единым контрактом» |
| 2.2g | Обновить FSM | ~L220-282 | Добавить preview-состояния (см. 2.1g) |
| 2.2h | Обновить ссылки на дочерние документы | ~L30-50 | Добавить ссылку на `pipeline1-formation_detail.md` |

### 2.3 `docs/pipelines/pipeline2-indexation.md` — **УМЕРЕННО ОБНОВИТЬ**

| # | Правка | Где | Что сделать |
|---|--------|-----|-------------|
| 2.3a | Переименовать сервис | Весь файл | `RAG Indexing` → `RAG Builder` |
| 2.3b | Уточнить вход | ~L40-50 | «Вход: плоский JSON от Registry (секции `nsi_document_sections`, не чанки)» |
| 2.3c | Уточнить шаг чанкования | ~L60-70 | «Разбиение **секций** на более мелкие **чанки** (не более 512 токенов)» |
| 2.3d | Обновить mermaid | ~L7-30 | Переименовать участника |

### 2.4 `docs/pipelines/pipeline3-search.md` — **МИНИМАЛЬНО**

| # | Правка | Где | Что сделать |
|---|--------|-----|-------------|
| 2.4a | Проверить naming | Весь файл | `RAG Service` → `RAG Search` (если ещё осталось) |
| 2.4b | Проверить ссылки | Весь файл | Убедиться, что вызов идёт к RAG Search, а не к «RAG Service» |

---

## Этап 3: API-документация

> **Зависимости**: Этап 2 завершён (названия сервисов и порты зафиксированы).
> 
> **Можно параллелить**: 3.1 + 3.2 + 3.3 + 3.4 + 3.5 — все независимы.

### 3.1 `docs/api/ocr_service_api.md` — **РАЗДЕЛИТЬ**

| # | Правка | Что сделать |
|---|--------|-------------|
| 3.1a | Создать `docs/api/parser_service_api.md` | Выделить всё, что касается цифровых PDF/DOC, в отдельный файл. Endpoint'ы: `POST /parser/process`, `POST /parser/preview`. Порт 8087. |
| 3.1b | Обновить `ocr_service_api.md` | Оставить только для сканов/изображений. Endpoint'ы: `POST /ocr/process`, `POST /ocr/preview`. Убрать упоминания Parser. Порт 8088. |
| 3.1c | Добавить preview-режим в оба | Параметр `max_pages` (по умолчанию 3). Preview-ответ: без `file_key`, без сохранения бинарных объектов. Полный ответ: все страницы, `file_key` для бинарных объектов. |
| 3.1d | Единый контракт выхода | Зафиксировать в обоих файлах идентичную структуру «сырого JSON» (плоские блоки, без иерархии). |

### 3.2 `docs/api/validate_service_api.md` → `docs/api/converter_validator_service_api.md`

| # | Правка | Что сделать |
|---|--------|-------------|
| 3.2a | Переименовать файл | `validate_service_api.md` → `converter_validator_service_api.md`. Порт 8086. |
| 3.2b | Добавить секцию «Preview API» | `POST /converter/preview/metadata` — вход: частичный сырой JSON → выход: обозначение, наименование, тип, даты, редакция. `POST /converter/preview/uniqueness` — вход: первичные метаданные → выход: список кандидатов-дубликатов с краткими карточками. |
| 3.2c | Добавить секцию «Full API» | `POST /converter/convert` — полная конвертация: построение иерархии, LLM, метаданные, перекрёстные ссылки. Вход: полный сырой JSON. Выход: иерархический типизированный JSON. |
| 3.2d | Сохранить существующие `/validate/*` | Как часть полного цикла (вызываются внутри `/converter/convert` или отдельно). |
| 3.2e | Добавить параметры LLM | `use_llm` (bool), `llm_model` (string), `llm_max_tokens` (int), `llm_timeout` (int, сек). |
| 3.2f | Обновить «Сводную информацию» | Отразить два режима (preview + full), использование LLM, отсутствие записи в БД. |

### 3.3 `docs/api/rag_service_api.md` → **РАЗДЕЛИТЬ НА ДВА**

| # | Правка | Что сделать |
|---|--------|-------------|
| 3.3a | Создать `docs/api/rag_builder_service_api.md` | `POST /rag/build` — вход: плоский JSON от Registry (секции). Процесс: чанкование секций → эмбеддинги → векторный индекс. `GET /rag/build/{doc_id}/status?longpoll=15`. `DELETE /rag/build/{doc_id}`. Порт 8090. |
| 3.3b | Создать `docs/api/rag_search_service_api.md` | `POST /rag/search` — гибридный поиск (dense + sparse + pg_trgm). Возвращает чанки с полным содержимым, без генерации LLM. Порт 8091. |
| 3.3c | Старый `rag_service_api.md` | Удалить или заменить на индексный файл со ссылками на два новых. |

### 3.4 `docs/api/orchestrator_service_api.md` — **ДОБАВИТЬ PREVIEW**

| # | Правка | Что сделать |
|---|--------|-------------|
| 3.4a | Добавить `POST /documents/{doc_id}/preview` | Запуск фазы превью. Возвращает `202 { preview_task_id, status: "previewing" }`. |
| 3.4b | Добавить `GET /documents/{doc_id}/preview/status` | Статус превью с longpoll. Ответ включает: `ocr_parser_status`, `converter_validator_status`, `metadata_extracted`, `duplicates_found`, `decision_required`. |
| 3.4c | Добавить `POST /documents/{doc_id}/decide` | Тело: `{ "action": "proceed" | "stop_duplicate" | "force_new_version", "comment": "..." }`. Возвращает `202` с новым статусом. |
| 3.4d | Обновить `GET /documents/{doc_id}/status` | В `steps.pipeline.formation` добавить блок `preview` с подполями: `status`, `ocr_parser`, `converter_validator`, `decision`. |
| 3.4e | Обновить FSM в ответах | Добавить `previewing`, `awaiting_decision` в возможные значения `status`. |

### 3.5 `docs/api/common.md` — **АКТУАЛИЗИРОВАТЬ**

| # | Правка | Что сделать |
|---|--------|-------------|
| 3.5a | Обновить «Обзор конвейера» (~L230-256) | Переписать Пайплайн 1 с учётом двух фаз: «Preview (проверка уникальности, метаданные) → решение пользователя → Full (конвертация, валидация, сохранение, индексация)» |
| 3.5b | Проверить «Стек технологий» (~L256-269) | Актуализировать при необходимости. |

---

## Этап 4: JSON-примеры и спецификации

> **Зависимости**: решения по открытым вопросам (bbox, `file_key`).

### 4.1 `docs/jsons/document1_parser.json`

| # | Правка | Что сделать |
|---|--------|-------------|
| 4.1a | `bbox` → 0..1 | Заменить пиксельные координаты на нормированные (разделить на `width`/`height` страницы). **Или** дождаться решения по вопросу #1. |
| 4.1b | `image_key` → `file_key` | Унифицировать имя поля для бинарных ссылок. |
| 4.1c | Проверить «плоскость» | Убедиться, что блоки не имеют полей `subsections`, `parent_id` — только плоский массив. |

### 4.2 `docs/jsons/document2_validate.json`

| # | Правка | Что сделать |
|---|--------|-------------|
| 4.2a | `bbox` → 0..1 | (см. 4.1a) |
| 4.2b | `image_key` / `file_key` → унифицировать `file_key` | Найти и заменить все варианты. |
| 4.2c | Добавить метку этапа | Явно указать в `metadata.schema`: `"converter_validator_full_v1"` |

### 4.3 `docs/jsons/document3_for_rag.json`

| # | Правка | Что сделать |
|---|--------|-------------|
| 4.3a | `bbox` → 0..1 | (см. 4.1a) |
| 4.3b | `image_key` → `file_key` | Унифицировать. |
| 4.3c | Добавить метку этапа | `metadata.schema`: `"registry_flat_v1"` — явно указать, что это выход Registry. |

### 4.4 Создать `docs/jsons/document2b_preview.json` (опционально)

| # | Правка | Что сделать |
|---|--------|-------------|
| 4.4a | Пример preview-ответа Converter-validator | Поля: `metadata` (обозначение, наименование, тип, даты), `duplicates` (массив кандидатов с краткими карточками), `decision_required: true`. |

### 4.5 `docs/jsons/document_container_purgatory.json`

| # | Правка | Что сделать |
|---|--------|-------------|
| 4.5a | `bbox` → 0..1 | (см. 4.1a) |
| 4.5b | Добавить примечание | Сверить структуру с эталонным выходом Converter-validator. |

### 4.6 `docs/specifications/parsing_specifications.md`

| # | Правка | Что сделать |
|---|--------|-------------|
| 4.6a | Добавить preview в `OCRPipeline` | Параметр `max_pages: int \| None` в `process()`. |
| 4.6b | Добавить `PreviewStateManager` | Или флаг `preview: bool` в существующий `StateManager`. |
| 4.6c | Добавить тест | `test_preview_mode_first_3_pages` |
| 4.6d | Обновить раздел 4 | «Что Orchestrator должен уметь» — добавить preview-вызовы. |

---

## Этап 5: Периферийные документы

> **Зависимости**: Этапы 2-3 завершены (можно параллелить).

### 5.1 `docs/discussions/pipeline1-formation_discussion.md`

| # | Правка | Что сделать |
|---|--------|-------------|
| 5.1a | Добавить плашку | `> **Статус**: исторический документ. Актуальные детали — в [pipeline1-formation_detail.md](../pipelines/pipeline1-formation_detail.md).` |
| 5.1b | Добавить перекрёстную ссылку | В начало файла. |

### 5.2 `docs/db_diagrams.md`

| # | Правка | Что сделать |
|---|--------|-------------|
| 5.2a | Добавить примечание | Указать, что preview-данные не хранятся в БД (живут в журнале Оркестратора). |
| 5.2b | Уточнить `nsi_document_sections` | Это **секции** (от Registry), не чанки. Чанки — в `nsi_chunks` (от RAG Builder). |
| 5.2c | Проверить `nsi_chunks` | Поле `section_id` должно ссылаться на `nsi_document_sections.id`. |

### 5.3 `docs/different_purgatory.md`

| # | Правка | Что сделать |
|---|--------|-------------|
| 5.3a | Актуализировать названия сервисов | В сравнительных таблицах заменить: Parsing/OCR → OCR + Parser, Validation → Converter-validator, RAG → RAG Builder + RAG Search. |
| 5.3b | Добавить примечание | Preview-фаза отсутствует в Purgatory, является нововведением `docs/`. |

### 5.4 `docs/jsons/diagrams.md`

| # | Правка | Что сделать |
|---|--------|-------------|
| 5.4a | Актуализировать mermaid-диаграммы | Отразить двухфазный пайплайн и новый состав сервисов (11 вместо 8). |

### 5.5 Остальные API-файлы — **ПРОВЕРИТЬ ССЫЛКИ**

| Файл | Что проверить |
|------|---------------|
| `docs/api/query_service_api.md` | Вызывает `RAG Search` (не «RAG Service»). Порт 8083. |
| `docs/api/integration_service_api.md` | Порт 8085. |
| `docs/api/auth_service_api.md` | Порт 8082. |
| `docs/api/analyse_service_api.md` | Порт 8089. |
| `docs/api/registry_service_api.md` | Порт 8084. |

---

## Сводка файловых операций

| Действие | Файлы |
|----------|-------|
| **Редактировать** | `pipeline1-formation_detail.md` (эталон — 5 правок) |
| **Переписать** | `pipeline1-formation.md` |
| **Существенно обновить** | `overview.md` |
| **Умеренно обновить** | `pipeline2-indexation.md`, `pipeline3-search.md`, `orchestrator_service_api.md`, `common.md`, `parsing_specifications.md` |
| **Создать** | `parser_service_api.md`, `converter_validator_service_api.md`, `rag_builder_service_api.md`, `rag_search_service_api.md`, `document2b_preview.json` (опционально) |
| **Удалить / deprecate** | `validate_service_api.md`, `rag_service_api.md` |
| **Редактировать JSON** | `document1_parser.json`, `document2_validate.json`, `document3_for_rag.json`, `document_container_purgatory.json` |
| **Добавить плашку** | `pipeline1-formation_discussion.md` |
| **Актуализировать** | `db_diagrams.md`, `different_purgatory.md`, `diagrams.md` |
| **Проверить ссылки** | `query_service_api.md`, `integration_service_api.md`, `auth_service_api.md`, `analyse_service_api.md` |

---

## Блокирующие зависимости

```
Решения по открытым вопросам (23_05_26.md §7)
        │
        ▼
   Этап 1: правка эталона
        │
        ▼
   Этап 2: архитектурные документы ──────┐
        │                                │
        ▼                                │
   Этап 3: API-документация ─────────────┤ (можно параллелить 3.1-3.5)
        │                                │
        ▼                                │
   Этап 4: JSON + спецификации ──────────┘
        │
        ▼
   Этап 5: периферия
```

---

## Оценка трудозатрат

| Этап | Файлов | Сложность | Примерное время |
|------|--------|-----------|-----------------|
| 1. Правка эталона | 1 | Низкая | 15 мин |
| 2. Архитектура | 4 | Высокая | 2-3 ч |
| 3. API | 7 | Высокая | 2-3 ч |
| 4. JSON/спеки | 7 | Средняя | 1-2 ч |
| 5. Периферия | 8 | Низкая | 30-45 мин |
| **Итого** | **27** | — | **6-9 ч** |