# План замены термина "промотирование" на семантику "завершение черновика → запись в Registry"

## Файлы для правок:

### 1. `docs/api/orchestrator_service_api.md`
- [x] Строка 3: `промотирование в Registry` → `запись результата в Registry`
- [x] Строка 18: `аппрув, промотирование` → `аппрув, завершение обработки`
- [x] Строка 36: `конвертация, промотирование` → `конвертация, завершение черновика`
- [x] Строка 72: `назначается Registry при промотировании черновика` → `назначается Registry при завершении черновика`
- [x] Строка 1014: `черновик промотируется в Registry (чистовик)` → `черновик завершается, документ записывается в Registry (чистовик)`
- [x] Строка 1092: `document_id после промотирования` → `document_id созданного документа`
- [x] Строка 1159: `document_id после промотирования` → `document_id созданного документа`
- [x] Строка 1301: `черновик либо промотируется в Registry` → `черновик либо завершается с записью в Registry`
- [x] Строка 1314: `approve — промотировать в Registry` → `approve — завершить черновик, записать документ в Registry`
- [x] Строка 1325: `Черновик промотирован в Registry. Запущен Пайплайн 2 (индексация).` → `Черновик завершён, документ создан в Registry. Запущен Пайплайн 2 (индексация).`
- [x] Строка 1350: `document_id после промотирования` → `document_id созданного документа`
- [x] Заголовок: `(готов к промотированию)` → `(готов к записи в Registry)`

### 2. `docs/api/common_api.md`
- [x] Строка 116: НЕ ТРОГАЕМ (promotion_task_id — поле API)
- [x] Строка 425: `не может быть промотирован в Registry` → `не может быть завершён — документ не будет создан в Registry`
- [x] Строка 577: НЕ ТРОГАЕМ (ready_for_promotion — имя статуса)

### 3. `docs/pipelines/pipeline1-formation.md`
- [x] Блок "Черновики (drafts):" строка 321: `Полная конвертация (validated_v3) запускается при промотировании.` → `Полная конвертация (validated_v3) запускается при approve, после чего документ записывается в Registry.`
- [x] Строка 120: `Черновик промотируется...` → `Черновик завершается...`
- [x] В Draft FSM: `автопромот не прошёл` → `автозавершение не прошло`
- [x] Таблица: `автопромот` → `автозавершение`
- [x] Таблица: `и промотирование в Registry` → `и запись документа в Registry`
- [x] В Draft FSM: `promoted --> [*] : документ в Registry` → НЕ ТРОГАЕМ
- [x] В FSM основной статусной модели: `ready_for_promotion --> registry : промотирование` → НЕ ТРОГАЕМ

### 4. `docs/pipelines/pipeline1-formation_detail.md`
- [x] `промотирование в Registry` → `завершение черновика, запись документа в Registry`

### 5. `docs/glossary.md`
- [x] Строка 67 (черновик): `автопромот в Registry` → `автоматическое завершение черновика с записью в Registry`
- [x] Строка 98: НЕ ТРОГАЕМ (ready_for_promotion)

### 6. `docs/plans/drafts_storage_plan.md`
- [x] `автопромот` → `автозавершение` / `автоматическое завершение`
- [x] `при промотировании` → `при завершении черновика`
- [x] `промотирование` → `завершение черновика, запись в Registry`
- [x] `auto_promote` → `auto_complete` (нода FSM)

### 7. `docs/README.md`
- [x] `промотирование черновика → распознавание` → `завершение черновика → распознавание`
- [x] `промотирован в Registry` → `завершён с записью в Registry`
- [x] `после промотирования` → `после завершения черновика`

### 8. Дополнительно (не указаны в задании, но содержали старую терминологию)
- [x] `docs/pipelines/pipeline2-indexation.md`: `не может быть промотирован` → `не может быть завершён — документ не будет создан в Registry`
- [x] `docs/specificity.md`: `блокировка промотирования` → `блокировка завершения черновика`
- [x] `docs/specificity.md`: `не может быть промотирован в Registry` → `не может быть завершён — документ не будет создан в Registry`

## НЕ ТРОНУТО (сознательно)
- `promoted` — статус черновика (API)
- `ready_for_promotion` — статус документа (API)
- `promoted_document_id` — поле ответа (API)
- `promotion_task_id` — поле ответа (API)
- FSM-метки с именами статусов (`ready_for_promotion --> registry : промотирование`)
