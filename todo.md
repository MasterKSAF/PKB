# Устранение случайного использования SQLite в оркестраторе

- [x] 1. `config.py` — убрать default для DATABASE_URL с SQLite, сделать обязательным
- [x] 2. `requirements.txt` — перенести aiosqlite в тестовые зависимости
- [x] 3. `db/base.py` — проверка, что DATABASE_URL задан (не пустой)
- [x] 4. `main.py` — блок ATTACH DATABASE только для SQLite (сейчас на все БД)
- [x] 5. `readme.md` — актуализировать
- [x] 6. `specificity.md` — зафиксировать изменения
- [x] 7. Перепроверка целостности

**Результат:** SQLite больше не является БД по умолчанию. Используется только при явном указании DATABASE_URL (локальная разработка / тесты). Production использует PostgreSQL.
