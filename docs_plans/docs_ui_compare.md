### Источники для сравнения

**UI Final (реализация):**

- 7 табов: `chat`, `search`, `documents`, `knowledgeProcessing`, `history`, `qa`, `admin`
- 13 компонентов, store, http-слой, access-контроль

**Документы:**

- `UI-UX/docs/customer-decisions.md` — решения заказчика
- `UI-UX/docs/backend-answers-ui-2026-05-08.md` — ответы backend-команды
- `UI-UX/UI Final/docs/ui-final-api-gap-fix-plan-2026-06-09.md` — план закрытия gap
- `UI-UX/UI Final/docs/ui-final-gateway-current-status-2026-06-03.md` — статус интеграции
- `UI-UX/UI Final/docs/knowledge-base-window-description-2026-06-05.md` — описание Базы знаний
- `docs/api/*.md` — API-контракты
- `docs/specificity.md` — аномалии

---

### 🔴 Критические

| #     | Расхождение                                    | Документ                                                                                                     | UI Final                                                                                                                        |
| ----- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **Search-таб не доступен ни для одной роли**   | `backend-answers-ui.md`: engineer: `["chat", "search", "checks", "history"]`                                 | `access.ts` — `search` отсутствует в `ROLE_TAB_ACCESS` у всех ролей. Компонент `Search.tsx` есть, но **недоступен в навигации** |
| **2** | **Черновики (drafts) не подключены к Gateway** | `gap-fix-plan.md` п.1: должен быть цикл `POST /drafts` → preview → `PATCH /drafts/{id}/decide` через Gateway | Черновики живут в React-состоянии, preview имитируется `setTimeout`, approve/reject локальные                                   |
| **3** | **Нет отдельной вкладки «Реестр»/«НСИ»**       | `backend-answers-ui.md` п.11: Registry — отдельная вкладка для `knowledgeAdmin` и `systemAdmin`              | В UI нет таба для CRUD классификаторов, терминологии, реестра НСИ                                                               |

---

### 🟡 Существенные

| #     | Расхождение                                 | Документ                                                                                                                                 | UI Final                                                                                  |
| ----- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| **4** | **Longpoll чата не используется**           | API: `GET /chat/sessions/{id}/messages/{msg_id}?longpoll=15` с FSM-статусами                                                             | UI ждёт ответ через запрос всей сессии, а не через longpoll конкретного сообщения         |
| **5** | **Demo/Gateway режимы не разделены жёстко** | `gap-fix-plan.md` п.3: в prod-режиме ошибки API не должны маскироваться mock-данными                                                     | Часть методов при ошибке молча возвращает mock-данные                                     |
| **6** | **Чат: нет привязки документов к сессии**   | API: `POST /chat/sessions` принимает `document_ids`, `options.model`, `options.temperature`                                              | UI создаёт сессию без документов и настроек модели                                        |
| **7** | **Нет «Проверки» (checks)**                 | `backend-answers-ui.md`: в `available_tabs` engineer есть `checks`. Во `frontend-v1` был `ChecksPanel`                                   | Явно удалён из UI Final. В `access.ts` нет таба `checks`                                  |
| **8** | **Экспорт истории не подключён к Gateway**  | API: `GET /chat/history/export`, `POST /chat/sessions/{id}/export`                                                                       | UI экспортирует CSV локально (из mock-данных), не через Gateway API                       |
| **9** | **Справочные Registry API не используются** | `gateway-current-status.md`: `/classifiers`, `/terminology`, `/registry/documents`, `/stats`, `/enums` — есть, UI частично не использует | UI использует только `/classifiers/tree`. CRUD классификаторов и терминологии отсутствует |

---

### 🔵 Средние

| #      | Расхождение                                                      | Документ                                                             | UI Final                                                                                                     |
| ------ | ---------------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| **10** | **DocumentRegistry.tsx — мёртвый компонент**                     | —                                                                    | Файл есть, но **не импортируется** в App.tsx. Вместо него используется `KnowledgeBase.tsx`. Возможно, legacy |
| **11** | **Кнопка «Повторить OCR» работает только для первого документа** | `gateway-current-status.md`                                          | UI запускает reprocess по первому документу, а не по выбранному                                              |
| **12** | **Роли как отдельный справочник**                                | API: `GET /admin/roles` существует                                   | UI не показывает роли отдельной таблицей — только в карточке пользователя                                    |
| **13** | **Артефакты и журналы обработки**                                | `gap-fix-plan.md` п.6: нужны подпункты в администрировании           | В админ-панели нет раздела «Артефакты»                                                                       |
| **14** | **Feedback — шкала и комментарий**                               | `gateway-current-status.md`: нужно уточнить шкалу                    | UI использует бинарную шкалу (like/dislike) с опциональным комментарием. Может не совпасть с финалом         |
| **15** | **Страница документа: detail/status/history/errors**             | `gap-fix-plan.md` п.5: для документа нужно подтягивать полные данные | Query есть в `KnowledgeBase`, но UI может не показывать все поля                                             |

---
