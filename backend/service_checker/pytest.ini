[pytest]
asyncio_default_fixture_loop_scope = module
markers =
    integration: интеграционные тесты, требующие Docker
    db_setup: тесты генерации SQL для инициализации БД
    real: тесты, выполняющиеся в real-режиме (против Docker)
    mock: тесты, выполняющиеся в mock-режиме (локальные моки)
