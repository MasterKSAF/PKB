# This file is intentionally minimal.
# docker-compose.yml already provides default values for the local service_checker stack.
# It exists so that clean-clone runs of check.bat / prepare.bat can safely create `.env`.

