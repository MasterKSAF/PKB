# API Coverage Test — что требуется исправить в сервисах

Собрано 2026-06-10 по результатам `api_coverage_test.py` (Docker, real mode).

---

## 1. Auth Service — 14/18 failed

| Проблема | Эндпоинты | Статус | Причина |
|----------|-----------|:------:|---------|
| **Admin endpoints не реализованы** | `/admin/users`, `/admin/roles`, `/admin/audit` (9 эндпоинтов) | 404 | В mock-режиме нет admin API |
| **`/auth/me` не реализован** | `/auth/me` (2 prepare + endpoint) | 404 | Нет эндпоинта профиля |
| **`/internal/auth/validate` не реализован** | `/internal/auth/validate` | 404 | Нет internal API |

**Что делать:** реализовать admin CRUD, `/auth/me`, `/internal/auth/validate`.

---

## 2. Registry Service — 23/35 failed

| Проблема | Эндпоинты | Статус | Причина |
|----------|-----------|:------:|---------|
| **`/documents` — 500** | create, list | 500 | Таблица `registry.documents` не создана (нет `create_all()` в startup) |
| **`/{classifier_code}` — 404** | get/update/patch/delete classifier | 404 | Prepare создаёт классификатор, но query-параметр `classifier_system` не передаётся или code не найден |
| **`/{doc_id}` — 404** | документы по ID | 404 | Документ не создан (предыдущий шаг упал с 500) |
| **`/{term_id}` — 404** | термины по ID | 404 | prepare создаёт, но get не находит |
| **`/pending/*` — 404** | карантин | 404 | Функционал карантина не реализован |
| **`/import` — 422** | импорт классификаторов/терминов | 422 | Тело запроса не проходит валидацию |
| **`/classifiers/` — 409** | create (prepare) | 409 | Дубликат — классификатор уже создан (но это OK) |

**Что делать:**
- Добавить `Base.metadata.create_all()` в startup Registry сервиса
- Исправить 500 при create/list документов
- Проверить валидацию `/import`
- Реализовать `/pending/*`

---

## 3. Converter-Validator — 4/4 failed

| Проблема | Эндпоинты | Статус | Причина |
|----------|-----------|:------:|---------|
| **`/health` — 404** | health check | 404 | Нет эндпоинта health |
| **Все POST — 422** | `/converter/*`, `/validate/*` | 422 | Тело запроса не проходит Pydantic-валидацию |

**Что делать:**
- Добавить `GET /health`
- Проверить Pydantic-схемы body: `task_id: int`, `version_id: int`, `raw_json: dict`

---

## 4. Parser Service — 5/6 failed

| Проблема | Эндпоинты | Статус | Причина |
|----------|-----------|:------:|---------|
| **Все POST — 422** | `/parser/process`, `/parser/preview` | 422 | Тело не проходит валидацию |
| **`/{task_id}/status` — 422** | статус парсинга | 422 | Параметр `task_id` не принимается как int |
| **`/{task_id}/result` — 422** | результат | 422 | Параметр `task_id` не принимается как int |

**Что делать:**
- Проверить Pydantic-схемы body: `task_id: int`, `version_id: int`, `file_key: str`
- Проверить path-параметр `task_id: int`

---

## 5. OCR Service — 6/6 failed

| Проблема | Эндпоинты | Статус | Причина |
|----------|-----------|:------:|---------|
| **Все эндпоинты 404** | все 5 эндпоинтов | 404 | OCR сервис не запущен (ping ❌) |

**Что делать:** запустить OCR сервис через supervisord.

---

## 6. Orchestrator — 0/30 failed ✅

**`/drafts/` — исправлено ✅** (был 422 → multipart/form-data с PDF вместо txt)

---

## 7. Query Service — 11/20 failed

| Проблема | Эндпоинты | Статус | Причина |
|----------|-----------|:------:|---------|
| **Все 422** | `/chat/sessions/{session_id}/*` | 422 | Path-параметр `session_id: int` не принимается |
| **POST `/chat/feedback` — 422** | feedback | 422 | Тело не проходит валидацию |

**Что делать:** проверить path-параметры `session_id: int`, `message_id: int`.

---

## 8. RAG Builder — 5/5 failed

| Проблема | Эндпоинты | Статус | Причина |
|----------|-----------|:------:|---------|
| **Все 422** | `/rag/build`, `/{doc_id}` | 422 | Тело не проходит валидацию или `doc_id: int` не принимается |

**Что делать:**
- Проверить body: `document_id: int`, `sections: list`
- Проверить path-параметр `doc_id: int`

---

## 9. RAG Search — 1/2 failed

| Проблема | Эндпоинты | Статус | Причина |
|----------|-----------|:------:|---------|
| **`/rag/search` — 500** | гибридный поиск | 500 | Индекс пуст (нет данных) или ошибка подключения к TEI |

**Что делать:** проверить интеграцию с TEI и пустой индекс.

---

## 10. TEI — 0/2 failed ✅ (исправлено)

| Проблема | Эндпоинты | Статус | Причина |
|----------|-----------|:------:|---------|
| **`/health` — 404 (было)** | health | ❌ | TEI не имеет `/health`, только `GET /` |
| **`response_schema` (было)** | health | ❌ | TEI возвращает модель, а не `{"status": str}` |

**Что исправлено (наша сторона):**
- Путь health check: `/health` → `/`
- Убран `response_schema` (TEI не возвращает фиксированную схему)

---

## 11. Gateway — 97/97 skipped

Gateway ping ❌ — сервис не отвечает на порту 8081.
Mock-сервис (`uvicorn gateway:app`) не запущен или падает при старте.
Чекер честно показывает недоступность, все эндпоинты пропущены.

**Статус:** не наша сторона.

## Сводка по типам проблем

| Тип | Кол-во | Корень |
|-----|:------:|--------|
| **404 — эндпоинт не реализован** | ~35 | Нет реализации в сервисе |
| **422 — невалидное тело/параметры** | ~25 | Pydantic схема не совпадает с реальным API |
| **500 — сервер падает** | ~5 | Нет `create_all()` в startup или ошибка в коде |
| **409 — дубликат** | 2 | OK, ожидаемое поведение |
| **Timeouts** | 1 | RAG Search долгий поиск |

**Исправлено со стороны чекера (теперь ✅):**
- TEI health: `/health` → `/`, убран `response_schema`
- Gateway/OCR: сообщение `❌ Сервис не отвечает` вместо вводящего в заблуждение `Зависит от`
- Orchestrator `/drafts/`: JSON → `multipart/form-data` + валидный PDF вместо txt
