# pyproject.toml

[project]
name = "rag-builder-spd"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]