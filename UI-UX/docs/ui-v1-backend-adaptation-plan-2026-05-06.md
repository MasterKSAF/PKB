# UI V1: план адаптации под backend API

Дата обновления: 2026-05-08
Ветка UI: `feature/ui-import-frontend-v1`
Основа: `docs/api`, `docs/ui_compare.md`, ответы backend-команды на вопросы UI

## 1. Что backend уже подтвердил

Эти пункты считаем рабочими решениями и больше не держим как открытые вопросы.

### Единый вход в API

Frontend ходит в единый публичный API:

```text
VITE_API_BASE_URL=https://{host}/api/v1
```

Для локального запуска:

```text
VITE_API_BASE_URL=http://localhost:{port}/api/v1
```

UI не должен напрямую выбирать между `Orchestrator`, `Query`, `Registry`, `Auth` и другими сервисами. Для frontend это один API-контур `/api/v1`, а маршрутизация по сервисам остается задачей backend.

### Роли и права

Backend предлагает три технические роли:

| Роль в API | Отображение в UI | Смысл |
|---|---|---|
| `engineer` | Инженер | Чат, поиск, проверка, история |
| `knowledge_admin` | Администратор НСИ | Все выше + загрузка/удаление документов, CRUD реестра, мониторинг |
| `system_admin` | Системный администратор | Полный доступ, пользователи, роли, аудит, реестры |

Вкладки и действия в UI берем не из ручной логики frontend, а из ответа `GET /auth/me`:

```json
{
  "role": "engineer",
  "role_title": "Инженер",
  "available_tabs": ["chat", "search", "checks", "history"],
  "permissions": {
    "can_upload_documents": false,
    "can_run_ocr": false,
    "can_manage_users": false,
    "can_manage_classifiers": false,
    "can_manage_terminology": false,
    "can_manage_registry": false
  }
}
```

### Чат

Основной сценарий для UI: `POST /chat`.

Если `session_id` не передан, backend создает чат-сессию автоматически. Явное создание через `POST /chat/sessions` остается дополнительным сценарием.

### История

Главный экран истории лучше строить вокруг чат-сессий:

- `GET /chat/sessions` - список сессий;
- `GET /chat/sessions/{session_id}` - полный диалог внутри сессии;
- `GET /chat/history` - плоский журнал отдельных вопросов и ответов.

В UI основной сценарий: открыть список сессий, раскрыть нужный диалог, при необходимости продолжить чат.

### Поиск по старым чатам

Сейчас backend описывает:

- поиск сессий по `title`: `GET /chat/sessions?search=<текст>`;
- фильтрацию плоской истории: `GET /chat/history?user_id=&status=&date_from=&date_to=`.

Полноценный full-text поиск по содержимому всех сообщений пока не описан.

### Документы: проектные и НСИ

Разделение идет по `document_type`:

| `document_type` | Категория | Назначение |
|---|---|---|
| `normative` | НСИ | ГОСТ, Правила РС и другие нормативные документы |
| `archival_scan` | НСИ | Архивные сканы |
| `drawing` | Проектный документ | Чертежи |
| `specification` | Проектный документ | Спецификации |

Для проверки НСИ backend ожидает раздельные списки:

```json
{
  "project_document_ids": ["doc-project-001"],
  "nsi_document_ids": ["doc-nsi-001", "doc-nsi-002"],
  "parameters": ["толщина листа", "марка стали"]
}
```

`user_id` берется из токена, а не передается из UI.

### Загрузка документов

Главный endpoint загрузки:

```text
POST /documents
```

Загрузка асинхронная. После нее backend запускает OCR, парсинг структуры и индексацию. Статус отслеживается через:

```text
GET /documents/{doc_id}/status
```

### `/documents` и `/registry/documents`

Это разные сущности, их нельзя смешивать.

| API | Что означает |
|---|---|
| `/documents` | Физический файл: загрузка, OCR, страницы, индекс, ошибки обработки |
| `/registry/documents` | Карточка НСИ-документа: название, номер, статус, классификатор, метаданные |

Связь должна идти через:

```text
documents.metadata.registry_doc_id -> registry.documents.doc_id
```

### Registry Service

Registry Service логически относится к вкладке `Реестр` / `НСИ` и административным функциям.

Для MVP backend закладывает:

- просмотр;
- создание;
- обновление;
- изменение статуса;
- удаление;
- импорт;
- экспорт.

Это относится к:

- НСИ-документам;
- классификаторам;
- терминологии.

## 2. Что уверенно меняем в UI V1

### HTTP-слой

Файл: `UI-UX/frontend-v1/src/utils/http.ts`

Сделать:

- добавить `VITE_API_BASE_URL`;
- все реальные запросы строить от `/api/v1`;
- добавить `Authorization: Bearer <access_token>`;
- оставить mock fallback для демонстрации без backend;
- сделать единый обработчик ошибок;
- поддержать разные варианты успешного ответа: прямой объект, `data`, `data + meta`.

Важно: в прежнем плане была жесткая обертка `{ success, data, error, meta }`. Теперь ее нельзя считать универсальной.

### Авторизация

Файлы:

- `src/components/LoginScreen.tsx`;
- `src/store/uiStore.ts`;
- `src/utils/access.ts`.

Сделать:

- demo-выбор пользователя оставить только для mock-режима;
- для real-режима использовать логин/пароль;
- после входа получать профиль через `GET /auth/me`;
- ФИО, должность, роль, вкладки и права брать из backend;
- кнопку выхода привязать к очистке токена и профиля.

### Роли, вкладки и кнопки

Файлы:

- `src/utils/access.ts`;
- `src/components/ModeSwitcher.tsx`;
- `src/App.tsx`;
- `src/components/AdminPanel.tsx`;
- `src/components/DocumentRegistry.tsx`.

Сделать:

- вкладки показывать по `available_tabs`;
- кнопки включать/выключать по `permissions`;
- для `engineer` скрывать отдельную вкладку `Реестр`, если backend не вернул ее в `available_tabs`;
- для `knowledge_admin` показывать функции НСИ и документов;
- для `system_admin` показывать администрирование пользователей, ролей и аудит.

### Чат

Файлы:

- `src/components/Chat.tsx`;
- `src/components/Feedback.tsx`;
- `src/utils/http.ts`;
- `src/store/uiStore.ts`.

Сделать:

- основной вызов перевести на `POST /chat`;
- хранить активный `session_id`;
- если `session_id` не пришел заранее, принимать тот, который вернул backend;
- нумерованные ответы строить из ответа backend;
- статусы `answered`, `needs_clarification`, `source_conflict` показывать разным, но спокойным UI-состоянием;
- feedback отправлять через `POST /chat/feedback`;
- `user_id` в feedback не передавать, если backend берет пользователя из токена.

Пока оставить:

- поиск по текущему открытому чату на frontend;
- mock-ответы для demo-режима.

### История

Файл: `src/components/History.tsx`

Сделать:

- главным представлением считать список чат-сессий;
- по клику раскрывать конкретный диалог через `GET /chat/sessions/{session_id}`;
- добавить действие `Продолжить чат`;
- плоскую историю `GET /chat/history` использовать как журнал/фильтр;
- экспорт истории отправлять с текущими фильтрами.

### Поиск

Файлы:

- `src/components/Search.tsx`;
- `src/utils/http.ts`.

Сделать:

- реальный поиск вести через `/documents/search`;
- результаты маппить в текущие карточки/таблицу UI;
- ссылки `Страница` и `Документ` брать из backend-полей preview/document URL.

### Страница и документ

Файлы:

- `src/components/SourcePreviewDialog.tsx`;
- `src/components/Chat.tsx`;
- `src/components/Search.tsx`;
- `src/components/ChecksPanel.tsx`.

Сделать:

- `Страница` открывает preview конкретной страницы;
- `Документ` открывает полный документ;
- если backend возвращает PDF/изображение/URL, UI показывает это в правой панели предпросмотра;
- исходный формат документа не подменять в UI без решения backend.

### Реестр и документы

Файлы:

- `src/components/DocumentRegistry.tsx`;
- `src/components/AdminPanel.tsx`;
- `src/utils/http.ts`.

Сделать:

- во вкладке документов не смешивать физические файлы и карточки НСИ;
- `/documents` использовать для загруженных файлов, OCR, индексации, ошибок и статусов;
- `/registry/documents` использовать для НСИ-карточек;
- связь показывать только если backend вернул `registry_doc_id`;
- действия CRUD/импорт/экспорт показывать только при соответствующих правах.

### Проверка НСИ

Файл: `src/components/ChecksPanel.tsx`

Сделать:

- выбор документов разделить на проектные и НСИ;
- отправлять `project_document_ids` и `nsi_document_ids`;
- `parameters` оставить необязательным фильтром;
- `user_id` не отправлять;
- результат проверки показывать в текущей таблице;
- экспорт результата привязать к endpoint backend, когда он будет подтвержден.

Важно: backend прямо указал, что функционал сверки пока не проработан. Значит, UI-сценарий оставляем, но реальную корректность проверки не обещаем до уточнения API.

### QA

Файл: `src/components/Monitor.tsx`

Сделать:

- оставить текущий UI;
- реальные метрики подключать только после подтверждения endpoint и состава полей;
- feedback инженеров учитывать как источник метрик качества, если backend возвращает такие агрегаты.

### Администрирование

Файл: `src/components/AdminPanel.tsx`

Сделать:

- пользователей брать из `/admin/users`;
- роли брать из `/admin/roles`;
- аудит брать из `/admin/audit`;
- смену роли отправлять через `/admin/users/{user_id}`;
- настройки классификаторов, терминологии и реестра НСИ показывать по правам.

## 3. Что пока подсвечиваем как открытые зоны

### Preview DOCX/XLSX/PPTX

Backend подтвердил, что поддержка DOCX/XLSX/PPTX для preview пока не описана.

В UI оставляем универсальный просмотр по ссылке/URL, но не обещаем корректный предпросмотр каждого офисного формата.

### Полнотекстовый поиск по чатам

Сейчас есть поиск сессий по заголовку и фильтрация истории. Full-text поиск по содержимому всех сообщений не описан.

Текущий поиск по открытому чату можно оставить на frontend, но поиск по старым чатам требует отдельного решения backend.

### Проверка НСИ

Endpoint `POST /validate/checks` обозначен, но backend пишет, что функционал сверки пока не проработан.

UI может показать сценарий, форму выбора документов и таблицу результата, но точная логика проверки остается открытой.

### Формат `POST /chat`

Backend подтвердил, что `POST /chat` основной для UI, но нужно закрепить финальный формат тела запроса и ответа.

Особенно важны поля:

- текст вопроса: `question`, `content` или другое имя;
- структура пунктов ответа;
- структура источников;
- `session_id`;
- `answer_id` / `message_id`;
- статусы `needs_clarification` и `source_conflict`.

### История для администратора

Не до конца ясно, видит ли администратор:

- только свои чаты;
- чаты всех пользователей;
- чаты только в пределах проекта/роли;
- обезличенную статистику без содержимого.

### Registry в навигации

Backend говорит, что для `engineer` вкладка `Реестр` не отображается, но чтение `/registry/*` может быть разрешено.

Нужно решить UI-поведение:

- скрывать вкладку полностью для инженера;
- показывать НСИ только как фильтр/источник в поиске;
- показывать реестр только администраторам.

## 4. Новый список вопросов backend-команде

### 1. Формат ответа API

Подтвердите финальное правило для успешных ответов:

- когда ответ приходит напрямую;
- когда используется `data`;
- когда используется `meta`;
- есть ли endpoint'ы, где еще встречается `success`.

Нужно для универсального обработчика в `http.ts`.

### 2. `POST /chat`

Просим закрепить финальный пример запроса и ответа для UI:

```json
{
  "session_id": null,
  "question": "Какая минимальная толщина листа корпуса?",
  "document_ids": ["doc-001"]
}
```

Нужно понять точные имена полей и структуру ответа:

- `session_id`;
- `answer_id` или `message_id`;
- `status`;
- пункты ответа;
- источники;
- ссылки на страницу и документ.

### 3. Автозаголовок чат-сессии

Кто формирует `title` сессии:

- frontend;
- backend;
- LLM;
- пользователь вручную?

Это влияет на экран истории и поиск по сессиям.

### 4. Поиск по содержимому старых чатов

Планируется ли endpoint для full-text поиска по сообщениям:

```text
GET /chat/search?q=...
```

Если нет, то UI оставляет только поиск по текущему открытому чату и поиск сессий по заголовку.

### 5. Права администратора на историю

Что может видеть `knowledge_admin` и `system_admin`:

- только свои сессии;
- все сессии;
- сессии по проекту;
- только агрегированные метрики?

Нужно для фильтров истории и QA.

### 6. Preview офисных документов

Как backend будет отдавать DOCX/XLSX/PPTX:

- оригинальным файлом;
- PDF-конверсией;
- HTML-preview;
- изображением страницы;
- ссылкой на внешний viewer?

Нужно для правой панели предпросмотра.

### 7. Страница документа

Что именно возвращает endpoint страницы:

- изображение страницы;
- PDF-страницу;
- текст OCR;
- координаты подсветки;
- готовый `preview_url`.

Нужно для кнопки `Страница`.

### 8. Проверка НСИ

Когда будет зафиксирован контракт `POST /validate/checks`:

- синхронный или асинхронный запуск;
- какие статусы результата;
- как выглядит `items[]`;
- как выглядят ссылки на проектный источник и источник НСИ;
- какой endpoint экспорта Excel.

### 9. Списки документов для проверки

Откуда UI должен брать проектные документы и НСИ-документы:

- один общий `/documents` с фильтром `document_type`;
- отдельный `/registry/documents`;
- смешанный список с признаком категории?

Нужно для вкладки `Проверка`.

### 10. Связь `/documents` и `/registry/documents`

Будет ли в ответе `/documents` поле:

```text
metadata.registry_doc_id
```

И будет ли endpoint для связывания/отвязывания физического файла с карточкой НСИ?

### 11. Registry import/export

Подтвердите форматы:

- импорт: XLSX, CSV или оба;
- экспорт: CSV, XLSX или оба;
- нужен ли mapping-файл;
- какие ошибки возвращаются при невалидном импорте.

### 12. QA-метрики

Какие метрики реально отдаст backend:

- качество OCR;
- качество retrieval;
- доля ответов с источниками;
- среднее время ответа;
- оценки инженеров;
- открытые вопросы;
- журнал проверки.

Нужно понять, какие блоки QA оставляем реальными, а какие пока demo.

### 13. Feedback

Подтвердите, что `POST /chat/feedback` принимает UI-формат:

```json
{
  "answer_id": "ans-001",
  "useful": true,
  "comment": "Ответ точный"
}
```

И возвращает агрегаты, влияющие на QA-метрики.

### 14. Загрузка документов

Подтвердите поля статуса обработки:

- стадия OCR;
- стадия парсинга;
- стадия индексации;
- прогресс;
- ошибки;
- время завершения.

Нужно для вкладки `Реестр` / загрузки документов.

## 5. Приоритет внедрения

### Сначала

1. Обновить `http.ts` под `/api/v1`, token и разные форматы ответа.
2. Подключить реальные роли и права через `GET /auth/me`.
3. Перевести чат на `POST /chat` с `session_id`.
4. Перестроить историю вокруг чат-сессий.

### Потом

1. Подключить поиск `/documents/search`.
2. Подключить preview страницы и документа.
3. Разделить `/documents` и `/registry/documents` в UI.
4. Привязать feedback к `POST /chat/feedback`.

### После уточнений

1. Реальная проверка НСИ через `/validate/checks`.
2. Full-text поиск по старым чатам.
3. Preview DOCX/XLSX/PPTX.
4. Реальные QA-метрики.
5. Импорт/экспорт Registry.

## 6. Короткий вывод

UI V1 оставляем базовой рабочей версией. По ответам backend уже можно уверенно уточнить роли, единый API-контур, чат-сессии, историю, загрузку документов и разделение `/documents` с `/registry/documents`.

Главные открытые риски: проверка НСИ, preview офисных файлов, full-text поиск по старым чатам и точный формат ответа `POST /chat`.
